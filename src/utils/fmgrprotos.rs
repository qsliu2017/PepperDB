//! Translated from PostgreSQL src/include/utils/fmgrprotos.h
//!
// GENERATED header (Gen_fmgrtab.pl from catalog/pg_proc.dat); it is just a list
// of `extern Datum <name>(PG_FUNCTION_ARGS);` prototypes. Per
// generated-header-files.md these are "not needed in Rust" - Rust has no separate
// declaration step, and the builtin function table is emitted by build.rs over
// pg_proc.dat with `#[builtin]` registration. Tombstone: no hand translation.

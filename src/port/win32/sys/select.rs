//! port/win32/sys/select.h - Windows compat shim for <sys/select.h> (empty)
//!
//! The PostgreSQL source header `src/include/port/win32/sys/select.h` contains
//! only its own path comment and defines no typedefs, structs, macros, or
//! prototypes. It exists solely to satisfy `#include <sys/select.h>` on the
//! Windows/MSVC build, where the platform provides no such header. Translated
//! faithfully as an empty module (0 symbols).

//! port/openbsd.h - OpenBSD platform-specific port header.
//!
//! Faithful translation of PostgreSQL 18.3 `src/include/port/openbsd.h`.
//!
//! The original C header contains no preprocessor directives, type
//! definitions, macros, or declarations - only the standard PostgreSQL
//! source-path comment marker (`/* src/include/port/openbsd.h */`).
//! It exists as a platform hook included by `pg_config_os.h` selection,
//! but defines nothing OpenBSD-specific in this PostgreSQL version.
//!
//! There is therefore no Rust-meaningful content to translate.

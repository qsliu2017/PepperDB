//! Translated from PostgreSQL src/include/pch/postgres_pch.h
//
// TOMBSTONE: precompiled-header aggregator (the C header is just
// `#include "postgres.h"`). Precompiled headers have no analogue in Rust -- the
// compiler caches across the crate. Nothing to translate; use `crate::postgres`.

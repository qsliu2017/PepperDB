| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| name | NEEDS_MODULES | `name` type eq/ne/lt/le/gt/ge on a table col; regex match `~`/`!~` on name; `parse_ident()`; `DO $$ ... $$` PL/pgSQL block calling `RAISE NOTICE`/`format()` | 1) `src/backend/parser/scan.rs` (lexer) -- STUB: no dollar-quote (`$$...$$`) token at all, blocks every `DO $$ ... $$` block in the file; also no generic-operator token so `~`/`!~` cannot even be parsed. 2) `src/regex/regex.rs` -- STUB: `pg_regcomp`/`pg_regexec` are `unimplemented!()`, and no `textregexeq`/`texticregexeq` function exists in `src/backend/utils/adt/varlena.rs`, so the `~`/`!~`/`~*`/`!~*` operators have no backing implementation even if parsed. 3) `parse_ident` -- MISSING: no such SQL-callable function anywhere in `src/` (only an unrelated `parse_ident` in `src/libpq/hba.rs`). | self-contained (creates NAME_TBL) |

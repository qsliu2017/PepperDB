| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| pg_lsn | NEEDS_MODULES | pg_lsn type (CREATE TABLE/INSERT/cast, `pg_input_is_valid`/`pg_input_error_info`, MIN/MAX agg, `=,!=,<,>,-,+` ops incl. numeric interop, btree/hash opclass via DISTINCT+ORDER BY, EXPLAIN) | src/backend/utils/adt/pg_lsn.rs (MISSING) -- no pg_lsn type/input-output/ops anywhere in src/backend | self-contained |
| plancache | NEEDS_MODULES | PREPARE/EXECUTE + plan invalidation on DROP/ALTER TABLE, temp views, `language plpgsql` functions incl. SPI DDL inside a function, search_path-sensitive replan, sequence regclass invalidation, `partition by list` + partition attach/detach, `pg_prepared_statements`, `plan_cache_mode` GUC (auto/force_generic_plan/force_custom_plan) | ref/postgres src/pl/plpgsql (MISSING -- no PL/pgSQL interpreter in src/); PARTITION BY (MISSING -- tablecmds.rs has no partitioning, only inheritance stub); src/backend/utils/cache/plancache.rs (STUB -- `RevalidateCachedQuery` at line 152 is `unimplemented!("...invalidation-driven re-analysis not yet reachable")`, so even the non-plpgsql/non-partition parts of the test cannot complete) | self-contained (temp tables built from int8_tbl) |
| plpgsql | OUT_OF_SCOPE | Entire PL/pgSQL language: functions/triggers with procedural bodies, control flow (IF/LOOP/WHILE/FOR/EXCEPTION), variables/records/RETURN NEXT, cursors inside PL/pgSQL, RAISE, dynamic SQL (EXECUTE), composite/row types, trigger functions firing on INSERT/UPDATE/DELETE | ref/postgres src/pl/plpgsql (MISSING -- no PepperDB module exists for PL/pgSQL compilation or execution; trigger firing exists but "user-PL bodies NOT executed" per project status) | self-contained (creates own Room/WSlot/PField/PSlot/etc. schema) |
| point | NEEDS_MODULES | `point` type I/O, operators `<<,>>,|>>,<<|,~=,<@,@>,<->`, `box`/`path` literals interacting with point, `pg_input_is_valid`/`pg_input_error_info`, GiST index on point (`USING gist`), enable_seqscan/indexscan/bitmapscan GUCs | src/backend/utils/adt/geo_ops.rs equivalent (MISSING -- no point/box/path/geometry type module anywhere under utils/adt); access/gist (MISSING -- only nbtree AM exists, no GiST) | point_tbl (from test_setup.sql, itself needs point type) |
| polygon | OUT_OF_SCOPE | `polygon` type I/O, operators `<<,&<,&&,&>,>>,<<|,&<|,|&>,|>>,<@,@>,~=,<->`, `polygon()`/`circle()` constructor functions, SP-GiST index (`USING spgist`), `pg_input_is_valid`/`pg_input_error_info`, rank() window fn over index vs seqscan comparison | src/backend/utils/adt/geo_ops.rs equivalent (MISSING -- no polygon/circle type support); access/spgist (MISSING -- non-btree index AM absent entirely) | self-contained |
| polymorphism | OUT_OF_SCOPE | Polymorphic SQL functions/aggregates (anyelement/anyarray/anyrange/anycompatible family), `language sql` and `language plpgsql` function bodies actually executed, polymorphic aggregate transition functions, VARIADIC, ANYRANGE/ANYCOMPATIBLERANGE/ANYCOMPATIBLEMULTIRANGE types | src/backend/executor/functions.rs equivalent (MISSING -- no SQL-language function body executor exists under src/backend/executor, so even non-plpgsql `language sql` bodies in this test cannot run); ref/postgres src/pl/plpgsql (MISSING, ~2 functions use it); range/multirange types (MISSING -- no ranges.rs/multirangetypes.rs under utils/adt) | self-contained |
| portals | NEEDS_MODULES | `DECLARE ... SCROLL/NO SCROLL/INSENSITIVE/BINARY CURSOR`, `FETCH`/`FETCH BACKWARD`/`FETCH ABSOLUTE`/`MOVE`, `WITH HOLD` cursors across COMMIT, `pg_cursors`, `WHERE CURRENT OF` (updatable cursors, DELETE/UPDATE), table inheritance (`INHERITS`), `CREATE FUNCTION ... AS 'DECLARE ... CURSOR'` (SQL-lang), toasted array fetch + `default_toast_compression='pglz'` | src/backend/tcop/pquery.rs (STUB -- `PortalRunSelect` backward-scan/scrollable-cursor path is `unimplemented!` at line 303, and `PortalStart` holdStore strategies are `unimplemented!` at line 165, blocking every `SCROLL`/`BACKWARD`/`WITH HOLD` cursor in this test from the very first statements); src/backend/executor/execCurrent.rs (MISSING -- no WHERE CURRENT OF support); src/backend/commands/tablecmds.rs (STUB -- `not_yet_reachable("DefineRelation: inheritance")` blocks `INHERITS`); src/backend/executor/functions.rs equivalent (MISSING -- SQL-lang function body with embedded DECLARE CURSOR can't execute) | tenk1, tenk2, onek (from test_setup.sql) |
| portals_p2 | READY | `DECLARE CURSOR` (default, non-scroll forward-only) + `FETCH ALL`/`CLOSE` over simple `SELECT * FROM onek/onek2 WHERE unique1 = N`, inside a transaction block | none -- only forward-only non-holdable cursors and plain equality-filtered SELECTs, both supported | onek, onek2 (from test_setup.sql) |

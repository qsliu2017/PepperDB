| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| create_table | OUT_OF_SCOPE | CREATE TABLE (UNLOGGED/TEMP/TEMP schema, AS SELECT, AS EXECUTE, extra-wide table, WITH OIDS rejection, default-expr validation, subtransaction rollback of relcache state); dominant feature is declarative PARTITION BY (LIST/RANGE/HASH)/PARTITION OF with bound checks, multi-column keys, operator classes, collations, column drop w/ partitions, domain-in-partition-key | access/partitioning (MISSING: `not_yet_reachable("DefineRelation: partitioning")` in src/backend/commands/tablecmds.rs -- whole subsystem, no partition bound catalog/pruning/attach-detach) | self-contained |
| create_table_like | OUT_OF_SCOPE | CREATE TABLE (LIKE ...) with INCLUDING DEFAULTS/CONSTRAINTS/IDENTITY/GENERATED/INDEXES/STORAGE/COMMENTS/STATISTICS/ALL, EXCLUDING ALL; INHERITS (classic multiple inheritance); GENERATED ALWAYS AS IDENTITY / STORED / VIRTUAL; CREATE FOREIGN TABLE + FDW/SERVER; CREATE STATISTICS; ALTER ... SET STORAGE | (1) src/backend/parser/parse_utilcmd.rs -- STUB, `Node::TableLikeClause(_) => not_yet_reachable("transformCreateStmt: LIKE clause")`; (2) INHERITS -- MISSING, no inheritance handling found in tablecmds.rs; (3) FDW/FOREIGN TABLE -- MISSING, no foreign.rs/fdw module; (4) CREATE STATISTICS -- MISSING, no statscmds module | self-contained |
| create_type | OUT_OF_SCOPE | CREATE TYPE base type (INPUT=/OUTPUT=/typmod_in/out/internallength/alignment/default/passedbyvalue), shell types, CREATE FUNCTION ... LANGUAGE C (dynamic loading of regress.so) and LANGUAGE internal aliasing, CREATE OPERATOR, box/point geometry, format_type(), pg_input_is_valid(), DOMAIN over user type, ALTER TYPE SET (send/receive/analyze/subscript/storage) | (1) src/backend/commands/typecmds.rs -- STUB, `define_type` hits `not_yet_reachable("DefineType: base type (INPUT=/OUTPUT=)")` for any non-composite/enum CREATE TYPE; (2) LANGUAGE C dynamic loading -- MISSING, no dlopen/CREATE FUNCTION...LANGUAGE C path in functioncmds.rs; (3) CREATE OPERATOR -- MISSING, no operatorcmds module; (4) geometry (point/box) -- MISSING, no geo.rs; (5) src/backend/utils/adt (format_type, pg_input_is_valid) -- MISSING, no matches anywhere | self-contained |
| create_view | NEEDS_MODULES (borderline OUT_OF_SCOPE) | CREATE VIEW/OR REPLACE/TEMP, view WITH(security_barrier/security_invoker) reloptions, CREATE RULE, pg_get_viewdef/pg_get_ruledef (ruleutils deparse), schema-qualified + nested CREATE SCHEMA...CREATE TABLE, natural/using/full joins in views, plpgsql SETOF-returning function referenced in a view, geometry fixtures (road/ihighway/path/point via `?#` operator, interpt_pp C function) | (1) src/backend/utils/adt/ruleutils.rs -- STUB, only has quote_identifier/generate_relation_name/get_rule_expr skeleton + `pg_get_constraintdef` unimplemented!; no `pg_get_viewdef`/`get_query_def`/`pg_get_ruledef` at all -- most-used function in this test; (2) src/backend/commands/view.rs -- STUB, `not_yet_reachable("DefineView: view WITH options (reloptions)")` blocks security_barrier/security_invoker tests; (3) CREATE RULE -- MISSING, no rulecmds module; (4) geometry (point/path/box, `?#` operator) -- MISSING, no geo.rs; (5) PL/pgSQL body execution -- MISSING (tt14f() SETOF function) | road, ihighway, emp (test_setup, geometry-heavy) for the opening section; rest self-contained |
| database | NEEDS_MODULES | CREATE/ALTER/DROP DATABASE (rename, SET/RESET TABLESPACE, CONNECTION_LIMIT, OWNER TO), CREATE/DROP ROLE, REASSIGN OWNED BY, UPDATE pg_database.datacl with makeaclitem/array_fill | (1) src/backend/commands/dbcommands.rs -- STUB, only createdb/dropdb/database_oid_by_name/delete_catalog_row_by_oid exist; no AlterDatabaseStmt handling (rename/tablespace/connlimit/owner) at all; (2) CREATE ROLE / roles -- MISSING, no create_role handler (RoleSpec exists only as a GRANT grantee AST node); (3) REASSIGN OWNED -- MISSING, no handler found | self-contained |
| date | NEEDS_MODULES | date type I/O across all DateStyle settings (iso/ymd/dmy/mdy), range limits, date arithmetic (-, epoch/yesterday/today/tomorrow), EXTRACT/date_part (year/month/day/quarter/decade/century/millennium/isoyear/week/dow/isodow/doy/julian/epoch, incl. BC and infinity), DATE_TRUNC, make_date/make_time (incl. error cases), infinity/-infinity comparisons and isfinite(), pg_input_is_valid/pg_input_error_info non-throwing API | src/backend/utils/fmgr/fmgr.rs -- STUB, `InputFunctionCallSafe`/`DirectInputFunctionCallSafe` are `unimplemented!("... needs ErrorSaveContext (miscnodes)")`, which blocks the 5 pg_input_is_valid/pg_input_error_info calls; everything else (date parsing/formatting, extract_date, timestamp_trunc, make_date/make_time) is already implemented in src/backend/utils/adt/date.rs (2299 lines, no relevant unimplemented!) and timestamp.rs | self-contained (creates its own DATE_TBL) |
| dbsize | NEEDS_MODULES | pg_size_pretty(bigint), pg_size_pretty(numeric), pg_size_bytes(text) parsing incl. units (kB/MB/GB/TB/PB), case-insensitivity, negative numbers, decimal points, scientific notation, invalid-input error cases | src/backend/utils/adt (pg_size_pretty / pg_size_bytes) -- MISSING, no grep hits anywhere in src/backend for either function name; dbsize.c has no PepperDB counterpart file | self-contained (VALUES-only, no tables) |
| delete | NEEDS_MODULES | CREATE TABLE w/ SERIAL PRIMARY KEY, DELETE FROM ... AS alias ... WHERE, alias-shadows-original-name error check, DELETE returning row count, TOASTed value deletion via repeat('x',10000), char_length() | (1) repeat() SQL function -- STUB, fmgrtab entry `func_name: "repeat"` has `func: None` (unbound) in src/backend/utils/adt/varlena.rs; (2) TOAST -- MISSING, heapam.rs comments confirm "M2: no toast (the tuple fits...)" -- no out-of-line storage path exists, so the >2KB repeat('x',10000) value cannot be stored/updated the way the test expects | self-contained |

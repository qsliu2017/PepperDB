| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| groupingsets | OUT_OF_SCOPE | GROUP BY ROLLUP/CUBE/GROUPING SETS (nested), GROUPING()/GROUP_BY DISTINCT, ordered-set aggs (percentile_disc, rank WITHIN GROUP), string_agg/array_agg ORDER BY, window fns over grouping sets, HAVING pushdown, plpgsql SRF (gstest_data), lateral, hash/sort agg comparison | `src/backend/optimizer/plan/planner.rs` (grouping_planner: `not_yet_reachable("grouping_planner: grouping sets")`, also HAVING/subquery/CTE/set-op planning `not_yet_reachable`) - MISSING grouping-sets planning entirely; `src/backend/parser/parse_agg.rs` (GroupingFunc/ordered-set/FILTER paths `not_yet_reachable`) - STUB; `src/backend/executor/nodeAgg.rs` (AggStrategy::MIXED / grouping_sets asserted empty) - STUB; PL/pgSQL body execution (gstest_data function) - MISSING | self-contained (temp views/tables gstest1-5, gstest_empty) + `tenk1`/`onek`/`int8_tbl` |
| guc | NEEDS_MODULES | SET/SET LOCAL/RESET/SHOW GUCs incl. custom GUCs, transactions+savepoints scoping, DISCARD TEMP/ALL, LISTEN, CREATE ROLE/SET SESSION AUTHORIZATION, function-local SET options (plpgsql), current_setting()/pg_settings_get_flags()/pg_show_all_settings(), pg_listening_channels/pg_prepared_statements/pg_cursors | `src/backend/commands/...` DISCARD statement handling - MISSING (no `DiscardStmt` found); `current_setting`/`pg_settings_get_flags`/`pg_show_all_settings` builtin fns - MISSING (no matches in src/backend); LISTEN/NOTIFY (`ListenStmt`) - MISSING; CREATE ROLE (`CreateRoleStmt`) - MISSING; PL/pgSQL function body execution (function-local SET tests, error rollback test) - MISSING | self-contained |
| hash_func | OUT_OF_SCOPE | hashint2/4/8, hashfloat4/8, hashoid, hashchar, hashname, hashtext, hashoidvector, hash_aclitem, hashmacaddr(8), hashinet, hash_numeric, hash_array, hash_record, hashbpchar, time/timetz/interval/timestamp_hash, uuid_hash, pg_lsn_hash, hashenum, jsonb_hash, hash_range/multirange, *_extended (seeded) variants, `bit`/`varbit`, `mood` ENUM type | `access/hash` fmgr hash function library (`hashfunc.c`, PG has no `src/backend/utils/adt/hashfunc.rs`) - MISSING entirely (only `hashint8`-equivalent logic inlined ad hoc in date.rs/timestamp.rs, and timestamp/interval/timestamptz `_hash` fns are `unimplemented!`); also needs jsonb, arrays, uuid, macaddr(8), inet, ranges/multiranges, record hashing, ENUM hashing - all untranslated | self-contained |
| hash_index | OUT_OF_SCOPE | CREATE INDEX USING HASH, hash opclasses (int4_ops/name_ops/text_ops/float8_ops), partial/functional/expressional hash indexes, fillfactor, bucket overflow/split/squeeze, REINDEX, backward cursor scan over hash index | `access/hash` index AM (PG `src/backend/access/hash/*.c`) - MISSING (only `src/backend/access/nbtree` exists; no hash dir under `src/backend/access`) | self-contained (own CREATE TABLE + COPY from data/hash.data) |
| hash_part | OUT_OF_SCOPE | `PARTITION BY HASH`, partition creation `FOR VALUES WITH (MODULUS,REMAINDER)`, `satisfies_hash_partition()` builtin, custom opclasses (part_test_int4_ops/part_test_text_ops from test_setup) | table partitioning (`PARTITION BY`) - MISSING entirely (no partitioning support found in codebase, per project status notes); `satisfies_hash_partition` builtin - MISSING | test_setup (part_test_int4_ops / part_test_text_ops custom opclasses) |
| horology | OUT_OF_SCOPE | timestamp/timestamptz/date/time/timetz literals incl. Julian dates, epoch/infinity, date+time/interval arithmetic, OVERLAPS operator, to_timestamp()/to_date()/to_char() format codes, named IANA timezones (America/Los_Angeles, America/New_York, MET DST, MSK, LMT) and POSIX TZ rules, SET/SHOW TIME ZONE | `src/backend/utils/adt/datetime.rs` + `src/backend/utils/adt/timestamp.rs` named-zone / IANA tzdb resolution - STUB (explicitly `unimplemented!`/STAGED: "named-zone (pg_tzset) ... IANA tz DB isn't ported", affects AT TIME ZONE, timestamptz arithmetic across DST, to_char TZ code, dynamic abbreviations MSK/LMT); OVERLAPS operator - MISSING from grammar (`gram.lalrpop` has no OVERLAPS); also depends on non-temp fixture tables (TIMESTAMP_TBL, date_tbl, INTERVAL_TBL, TIME_TBL, TIMETZ_TBL, TIMESTAMPTZ_TBL) created by sibling tests date/time/timetz/timestamp/timestamptz/interval earlier in the schedule, not test_setup.sql | date_tbl/TIMESTAMP_TBL/TIMESTAMPTZ_TBL/INTERVAL_TBL/TIME_TBL/TIMETZ_TBL (created by earlier sibling test files date.sql/time.sql/timetz.sql/timestamp.sql/timestamptz.sql/interval.sql, not test_setup.sql) |
| identity | OUT_OF_SCOPE | `GENERATED {ALWAYS\|BY DEFAULT} AS IDENTITY` column DDL (CREATE/ALTER TABLE ADD/DROP/SET), OVERRIDING SYSTEM/USER VALUE on INSERT/MERGE, identity+partitioning interaction, identity+views, information_schema.columns identity fields, pg_get_serial_sequence | GENERATED ... AS IDENTITY grammar - MISSING (`gram.lalrpop` has no GENERATED/IDENTITY tokens); catalog only has `attidentity` storage plumbing in `src/backend/catalog/heap.rs`/`src/backend/commands/tablecmds.rs` (field exists but no parser/DDL path sets it) - STUB; also needs table partitioning (pitest* tests) and inheritance (itest7*) - MISSING | self-contained |
| incremental_sort | OUT_OF_SCOPE | Incremental Sort plan node (mode-transition/group-based sort), EXPLAIN ANALYZE JSON output with plpgsql parsing of plan tree, parallel query (Gather/GatherMerge, parallel workers), GiST index + `<->` distance ordering (amcanorderbyop), rescan of incremental sort under nestloop | Incremental Sort executor/planner node - MISSING entirely (no `IncrementalSort`/`incremental_sort` hits anywhere in `src/backend`); parallel query executor (Gather/GatherMerge workers) - STUB/MISSING beyond planner.rs mentions; `access/gist` index AM - MISSING (no gist dir) | self-contained (`t`/`point_table`) + `tenk1` |

| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| select_distinct | NEEDS_MODULES | SELECT DISTINCT / DISTINCT+ORDER BY USING, IS DISTINCT FROM, CTAS (`CREATE TABLE ... AS SELECT`), `generate_series(int,int)`, inheritance query `person*`, GUC toggles (enable_hashagg/enable_sort/parallel_*), incremental sort, EXPLAIN | src/commands/createas.rs (STUB: `ExecCreateTableAs` = `unimplemented!()`, needed for `CREATE TABLE distinct_group_1/2/hash_1/2 AS SELECT`); src/funcapi.rs (STUB: SRF multi-call context, needed by `generate_series(0,9999)`); access/inheritance in src/backend/commands/tablecmds.rs (STUB: `not_yet_reachable("DefineRelation: inheritance")`, needed for `person*` query, though this specific query is over test_setup's person/emp/student hierarchy) | onek, tenk1, person/emp/student (inheritance) |
| select_distinct_on | NEEDS_MODULES | `SELECT DISTINCT ON (...)`, ORDER BY USING, `random()`, `floor()`, incremental sort, index usage, EXPLAIN | none beyond already-translated DISTINCT ON support; self-contained table `distinct_on_tbl` created fresh (no CTAS needed here -- uses plain CREATE TABLE + INSERT SELECT generate_series); src/funcapi.rs (STUB, needed for `generate_series(1,1000)` used to populate distinct_on_tbl) | onek, tenk1, int4_tbl |
| select_having | READY | GROUP BY/HAVING (with and without GROUP BY), aggregates (count/min/max), `lower()` on char, degenerate HAVING without GROUP BY, ungrouped-column errors | none -- char(n)/int types, GROUP BY/HAVING, count/min/max all translated; self-contained table | self-contained (test_having) |
| select_implicit | NEEDS_MODULES | implicit GROUP BY/ORDER BY via resjunk targets, ordinal GROUP BY/ORDER BY refs, ambiguous-column errors, `lower()`/`upper()`, arithmetic expressions in GROUP BY, CTAS (`CREATE TABLE test_missing_target2/3 AS SELECT ...`) | src/utils/formatting.rs (STUB: `str_tolower`/`str_toupper` = `unimplemented!()`, needed for `lower(c)`/`upper(d)`); src/commands/createas.rs (STUB, needed for the two `CREATE TABLE ... AS SELECT ... GROUP BY` statements) | self-contained (test_missing_target) |
| select_into | NEEDS_MODULES | `SELECT ... INTO TABLE`, `CREATE TABLE ... AS SELECT [WITH [NO] DATA]`, `CREATE TABLE IF NOT EXISTS ... AS`, `EXECUTE ... WITH DATA`, `CREATE USER`/`SET SESSION AUTHORIZATION`/default privileges, `CREATE SCHEMA ... CASCADE`, SQL-language function body execution (`make_table()`), `generate_series`, disallowed-INTO error cases, `DECLARE CURSOR ... INTO` | src/commands/createas.rs (STUB: entire SELECT INTO / CTAS path is `unimplemented!()` -- this is the core feature under test); src/commands/user.rs (STUB: `CreateRole`=`unimplemented!()`, needed for `CREATE USER regress_selinto_user`); src/funcapi.rs (STUB, needed for `generate_series(1,3)`); PL/SQL-body execution for SQL-language functions (`make_table()` calls CREATE TABLE AS from within a SQL function body -- NOT TRANSLATED per project scope, function bodies are catalog-only) | self-contained (onek, onek2, int8_tbl referenced) |
| select_parallel | OUT_OF_SCOPE | real multi-process parallel query (Gather/Gather Merge, `debug_parallel_query`, `max_parallel_workers`, `parallel_leader_participation`), `pg_stat_database` parallel worker counters, table inheritance (a_star..f_star from create_misc.sql), partitioning (`PARTITION BY RANGE`), custom operator class (`CREATE OPERATOR CLASS ... USING btree`), `generate_series`, PL/pgSQL function bodies (`sp_parallel_restricted`, etc.) | whole subsystem: PepperDB is single-process async tokio (no background worker parallel executor) -- no `debug_parallel_query`/`max_parallel_workers` GUCs or `pg_stat_database` parallel columns found anywhere in src/; also needs INHERITS (STUB in tablecmds.rs), partitioning (NOT TRANSLATED), custom opclasses (MISSING), PL/pgSQL body execution (NOT TRANSLATED) | tenk1, tenk2, a_star.. f_star (inheritance, from create_misc.sql) |
| select_views | NEEDS_MODULES | `CREATE VIEW ... WITH (security_barrier)`, `CREATE ROLE`, leaky-view qual pushdown semantics (leakproof/security_barrier optimizer behavior), NATURAL JOIN, `current_user`, PL/pgSQL function body execution (`f_leak`), PREPARE/EXECUTE, `SET SESSION AUTHORIZATION` | src/commands/user.rs (STUB: `CreateRole`=`unimplemented!()`, needed for `CREATE ROLE regress_alice`); PL/pgSQL function body execution for `f_leak` (NOT TRANSLATED -- function bodies are catalog-only, not executed) -- the whole scenario hinges on this function actually running to prove leak/no-leak ordering, so it cannot conform even though security_barrier plumbing exists in the optimizer (`optimizer/prep/prepjointree.rs`, `parse_relation.rs`) | self-contained (customer/credit_card/credit_usage) |
| sequence | NEEDS_MODULES | CREATE/ALTER/DROP SEQUENCE incl. typed sequences (AS smallint/int/bigint), SERIAL/SMALLSERIAL/BIGSERIAL column shorthand, nextval/currval/setval/lastval, OWNED BY, sequence renaming, DISCARD SEQUENCES, information_schema.sequences, pg_sequences, pg_get_sequence_data, pg_get_serial_sequence, CREATE USER + GRANT/REVOKE on sequences, read-only-transaction sequence errors | grammar: SERIAL/SMALLSERIAL/BIGSERIAL/SERIAL2/SERIAL4/SERIAL8 column-type shorthand is MISSING (not present in src/backend/parser/gram.lalrpop or parse_utilcmd.rs -- no implicit-sequence column transform found), blocking `serialTest1`/`serialTest2`; `pg_get_serial_sequence` fn is MISSING (no hits in src/); `lastval()` fn is MISSING (no hits in src/); `pg_sequences` system view is MISSING; `information_schema.sequences` view is MISSING; `pg_get_sequence_data` fn is MISSING; src/commands/user.rs (STUB: `CreateRole`=`unimplemented!()`, needed for `CREATE USER regress_seq_user`); core CREATE/ALTER SEQUENCE + nextval/currval/setval in src/backend/commands/sequence.rs are already translated (real bodies, not stubs) | self-contained |

| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| insert | OUT_OF_SCOPE | INSERT w/ DEFAULT, multi-row VALUES, TOAST, WITH(fillfactor)+ALTER STORAGE, field/array-subscript indirection targets on composite/array/domain columns, RULEs, and (majority of file) declarative table PARTITION BY range/list/hash with tuple routing, multi-level partitioning, BR triggers on partitions, \d+ | commands/tablecmds.rs (`DefineRelation: partitioning` is `not_yet_reachable` -- whole PARTITION BY / partition-of / tuple-routing subsystem MISSING); parse_target.rs (no FieldStore/SubscriptingRef indirection-target handling for `col[1]`/`col.field` in INSERT target list) | self-contained (creates its own tables) |
| insert_conflict | NEEDS_MODULES | INSERT ... ON CONFLICT (col[,...] \| expr) [WHERE ...] DO NOTHING / DO UPDATE SET ... [WHERE ...], unique-index inference (opclass/collation-qualified), EXCLUDED.*, partial/expression unique indexes, aliasing target relation, EXPLAIN | src/backend/parser/analyze.rs (`transformInsertStmt: ON CONFLICT clause` = `not_yet_reachable`, STUB); src/backend/executor/nodeModifyTable.rs (`exec_init_modify_table` asserts `on_conflict_action == NONE`, i.e. no DO NOTHING/DO UPDATE codepath at all, STUB) | self-contained (creates insertconflicttest + view) |
| int2 | NEEDS_MODULES | int2 text I/O incl. malformed input, int2 vs int2/int4 comparison+arithmetic across widths, non-decimal (0b/0o/0x) + underscore literals, float8/numeric->int2 rounding, `pg_input_is_valid`/`pg_input_error_info` soft-error API, int2vector via `pg_input_is_valid` | src/backend/utils/fmgr/fmgr.rs (`InputFunctionCallSafe`/`DirectInputFunctionCallSafe` = `unimplemented!`, STUB -- blocks all `pg_input_is_valid`/`pg_input_error_info` calls); src/backend/utils/adt/int.rs (`int2vectorin`/`int2vectorout` = `unimplemented!`, STUB -- blocks int2vector validity checks) | int2_tbl (test_setup) |
| int4 | NEEDS_MODULES | int4 text I/O incl. malformed input, int2/int4 cross-width comparison+arithmetic, unary-minus parsing corner cases, INT_MIN overflow for mul/div/mod, float8/numeric->int4 rounding, gcd()/lcm(), non-decimal+underscore literals, `pg_input_is_valid`/`pg_input_error_info` | src/backend/utils/fmgr/fmgr.rs (`InputFunctionCallSafe`/`DirectInputFunctionCallSafe` = `unimplemented!`, STUB -- blocks `pg_input_is_valid`/`pg_input_error_info`); gcd/lcm/arithmetic/literal-parsing already implemented in src/backend/utils/adt/int.rs (not blocking) | int4_tbl (test_setup) |
| int8 | NEEDS_MODULES | int8 text I/O incl. malformed input, int8/int8/int4/int2 cross-width cmp+arithmetic, bitops, to_char(int8,...), CAST to int4/int2/float4/float8/oid, gcd()/lcm(), generate_series(int8[,step]), INT64_MIN overflow, non-decimal+underscore literals, `pg_input_is_valid`/`pg_input_error_info` | src/backend/utils/fmgr/fmgr.rs (`InputFunctionCallSafe` = `unimplemented!`, STUB -- blocks `pg_input_is_valid`/`pg_input_error_info`); src/backend/utils/adt/int.rs (`int8gcd`/`int8lcm` MISSING -- PG's int8.c has them, no Rust equivalent found); src/executor/nodeFunctionscan.rs (`unimplemented!` on all 3 fns, STUB -- FunctionScan node needed to run `SELECT * FROM generate_series(...)`) | int8_tbl (test_setup) |
| interval | OUT_OF_SCOPE | interval literal parsing (postgres/sql_standard/ISO8601 styles, typmod precision, year-to-month/day-to-second field restriction, non-decimal overflow edge cases), IntervalStyle/DATESTYLE GUCs, arithmetic incl. infinity/NaN, justify_hours/days/interval, make_interval, btree index + ORDER BY, avg(interval), EXTRACT(field FROM interval) incl. EPOCH, date_bin, timezone(interval,...), a user-defined `LANGUAGE plpgsql` helper function `eval()` invoked ~60 times via `EXECUTE ... INTO` | PL/pgSQL body execution (whole subsystem MISSING -- no plpgsql module found under src/backend at all; blocks the `eval()` function used throughout the back half of the file); src/backend/utils/adt/timestamp.rs (`extract_interval` = `unimplemented!`, STUB -- blocks all `EXTRACT(... FROM interval)` numeric-result calls, ~20+ uses) | self-contained (creates INTERVAL_TBL, INTERVAL_TBL_OF, INTERVAL_MULDIV_TBL, INFINITE_INTERVAL_TBL) |
| join | NEEDS_MODULES | broad classic-join surface (inner/cross/natural/USING/LEFT/RIGHT/FULL, multiway FULL JOIN, subquery pull-up, LATERAL, semijoin selectivity) plus advanced planner proofs: unique-join detection, GEQO, nbtree ScalarArrayOpExpr (SAOP) mark/restore + "find extreme element", bitmap-heap-scan skip-fetch, foreign-key join selectivity estimation, OR-to-SAOP transform, EXPLAIN (COSTS OFF) | src/backend/optimizer/plan/analyzejoins.rs (`innerrel_is_unique`/`innerrel_is_unique_ext` = `not_yet_reachable`, STUB -- blocks the ~20 "ensure join is properly marked as unique" tests, lines ~3558-3730); GEQO optimizer (MISSING -- no geqo*.rs found anywhere under src/backend/optimizer, blocks `set geqo = on` test at line 381); src/backend/access/nbtree (SAOP/array-keys noted as future work in nbtutils.rs doc comment, not implemented -- blocks array-key mark/restore + extreme-element + OR-to-SAOP tests, lines ~3687-3813). Core join execution (nestloop/hash/merge, bitmap heap scan, LATERAL) is already translated and not blocking. | self-contained (J1_TBL/J2_TBL/t1/t2/t3/and many more created inline); also reads onek/tenk1-style ad hoc tables it creates itself |
| join_hash | OUT_OF_SCOPE | hash-join batch/bucket growth and skew behavior driven entirely through two `LANGUAGE plpgsql` helper functions (`find_hash`, `hash_join_batches`) that call `EXPLAIN (ANALYZE, FORMAT 'json')` and walk the plan with `json_extract_path`/`json_array_elements`, plus parallel-query GUCs (`parallel_workers`, `max_parallel_workers_per_gather`, `min_parallel_table_scan_size`) | PL/pgSQL body execution (whole subsystem MISSING, needed for both helper functions that gate every test in the file); src/backend/utils/adt (no json.rs/jsonb.rs -- json type + `json_extract_path`/`json_array_elements` MISSING); parallel query workers (no evidence of parallel worker execution machinery, GUCs referenced throughout are load-bearing for the test's assertions) | self-contained (creates simple/bigger_than_it_looks/extremely_skewed/wide) |

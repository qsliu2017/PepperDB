| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| random | OUT_OF_SCOPE | random()/random_normal()/random(min,max) over float8/int4/int8/numeric, setseed(), erf(), Kolmogorov-Smirnov tests wrapped in `LANGUAGE plpgsql` functions, `count(*) FILTER (WHERE ...)`, generate_series | (1) whole PL/pgSQL execution subsystem -- MISSING (no plpgsql dir/module anywhere under src/); most of this file's logic (ks_test_uniform_random etc.) is inside `CREATE FUNCTION ... LANGUAGE plpgsql` bodies that never execute; (2) src/backend/utils/adt/float.rs -- STUB: no `random`/`random_normal`/`setseed` fns found at all (only `numeric_random` stub in numeric.rs at line 2521, `derf` unimplemented at float.rs:1358) | self-contained |
| rangefuncs | NEEDS_MODULES | SQL-language table functions (`CREATE FUNCTION ... LANGUAGE SQL` returning setof/table/record), `WITH ORDINALITY`, `ROWS FROM(...)`, `unnest()` multi-arg, lateral function calls, cursors (FETCH forward/backward), views over functions | src/executor/functions.rs -- STUB: `fmgr_sql` (SQL function body execution/`unimplemented!()` at line 25), `check_sql_fn_retval`, `prepare_sql_fn_parse_info`, `CreateSQLFunctionDestReceiver` all `unimplemented!()`; every query in this file calls a `LANGUAGE SQL` function so nothing runs until this lands | int4_tbl, int8_tbl |
| rangetypes | OUT_OF_SCOPE | range type input/output parsing, `numrange`/`int4range`/`daterange`/`textrange`/`float8range` types + constructor fns, range operators (`@>`,`<@`,`-|-`,`&&`, etc.), `range_adjacent`/`range_contains`/`isempty`/`lower`/`upper`/`lower_inc`, `CREATE TYPE ... AS RANGE`, btree index over range, `pg_input_error_info`/`pg_input_is_valid` soft-error API | (1) `CREATE TYPE ... AS RANGE` -- MISSING in src/backend/commands/typecmds.rs (no TYPTYPE_RANGE/CreateRange handling found); test_setup.sql's `create type textrange as range (...)` cannot even run; (2) built-in range types int4range/numrange/daterange/float8range -- MISSING (zero references anywhere in src/backend catalog bootstrap); (3) src/utils/rangetypes.rs is only the header/struct-layout stub (29 unimplemented!/not_yet_reachable sites), no src/backend/utils/adt/rangetypes.rs body file exists at all; (4) soft-error API `pg_input_error_info`/`pg_input_is_valid` -- MISSING, not found anywhere in src/backend | test_setup textrange/float8range types; otherwise self-contained (numrange_test table created inline) |
| regex | NEEDS_MODULES | `~`/`~*` regex match operators, `regexp_match`/`regexp_matches`, backreferences, lookahead/lookbehind, non-greedy quantifiers, `substring(... from ...)`, `explain (costs off)` for indexable regex conditions | (1) src/backend/utils/adt/regexp.c -- MISSING: no Rust translation exists anywhere (grepped src/backend/utils/adt for regexp_match/regexp_matches/regclass-style fns, zero hits) -- this is what implements `~`, `regexp_match`, `regexp_matches`, `textregexeq`; (2) src/regex/regex.rs -- STUB: the underlying Spencer regex engine itself has 7 `unimplemented!()`/`not_yet_reachable` sites, so even once regexp.c is ported the engine underneath needs finishing | self-contained (also touches pg_proc catalog for the EXPLAIN cases) |
| regproc | OUT_OF_SCOPE | `regoper`/`regoperator`/`regproc`/`regprocedure`/`regclass`/`regtype`/`regcollation`/`regrole`/`regnamespace` casts and their `to_reg*` soft-error counterparts, `pg_input_error_info`/`pg_input_is_valid`, `format_type`, `to_regtypemod`, `CREATE ROLE` | (1) whole regproc.c-equivalent module -- MISSING: zero references to regclass/regoper/regtype/regrole cast functions found anywhere in src/backend; (2) soft-error API `pg_input_error_info`/`pg_input_is_valid` -- MISSING everywhere in src/backend; (3) `CREATE ROLE`/roles(create_role/authid) -- known-absent per project scope notes, needed for `regress_regrole_test` | self-contained |
| reindex_catalog | NEEDS_MODULES | `REINDEX TABLE`/`REINDEX INDEX` on system catalogs (pg_class, pg_index, pg_operator, pg_database, pg_shdescription) and their indexes, mapped/shared/critical relation handling, parallel-scan GUC interaction inside a transaction | REINDEX statement handling -- MISSING: no `ReindexStmt`/`Reindex` execution found in src/backend/commands/indexcmds.rs or tcop dispatch (only a doc-comment mentions "ReindexIndex...are staged guards"); need REINDEX TABLE/INDEX driver (reindex_relation/ReindexIndex) targeting both regular and mapped/shared catalogs | self-contained (uses only bootstrap catalog relations, no test_setup fixtures) |
| reloptions | NEEDS_MODULES | `WITH (fillfactor=.., autovacuum_enabled=.., autovacuum_analyze_scale_factor=.., vacuum_truncate=.., toast.*=..)` on CREATE/ALTER TABLE and CREATE/ALTER INDEX, reloption validation (min/max bounds, type coercion, unknown option/namespace errors, duplicate-option errors), RESET, TOAST reloptions namespace, `pg_relation_size` | src/backend/access/common/reloptions.rs -- STUB: `transformRelOptions` explicitly `unimplemented!("... with a non-empty WITH list is staged")` (line 41) and `default_reloptions` `unimplemented!("... with non-empty options is staged (StdRdOptions framework)")` (line 54); every statement in this test supplies a non-empty WITH/SET/RESET option list, so nothing in the file can run past the option-parsing stage | self-contained |
| replica_identity | OUT_OF_SCOPE | `ALTER TABLE ... REPLICA IDENTITY {USING INDEX idx \| DEFAULT \| FULL \| NOTHING}`, `relreplident`/`pg_index.indisreplident`, `CREATE INDEX ... USING hash`, deferrable unique constraints as identity candidates, `PARTITION BY LIST`/`ATTACH PARTITION`, `ALTER INDEX ... ATTACH PARTITION`, `ALTER TABLE ... ALTER COLUMN ... TYPE` rewrite preserving identity | (1) `REPLICA IDENTITY` clause -- MISSING: no grammar rule and no ALTER TABLE handling anywhere in src/backend/parser/gram.lalrpop or commands/tablecmds.rs; only a fixed default `relreplident = 'd'` is set at table creation in catalog/heap.rs:114; (2) hash index AM -- MISSING (src/backend/access has only nbtree, no hash/gin/gist/spgist/brin dirs); (3) table partitioning (`PARTITION BY`, `ATTACH PARTITION`) -- MISSING (only window-function PARTITION BY exists in the grammar, no table partitioning) | self-contained |

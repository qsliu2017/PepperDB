| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| create_function_sql | OUT_OF_SCOPE | CREATE/ALTER/DROP FUNCTION LANGUAGE SQL with volatility/security/leakproof/strict attrs, SQL-standard body (`RETURN expr` / `BEGIN ATOMIC...END`), OUT/default params, `pg_get_functiondef`, information_schema.parameters/routines, SRF inlining, GRANT EXECUTE, CREATE USER | (1) src/backend/utils/fmgr/fmgr.rs STUB -- `fmgr_info_cxt_security` non-builtin path is `unimplemented!`, so any call to a created SQL-language function (e.g. `SELECT functest_A_1(...)`) panics; this is the core capability the whole test exercises. (2) src/backend/commands/functioncmds.rs STUB -- `create_function` hits `not_yet_reachable` on non-IN parameter mode (OUT/INOUT), parameter DEFAULT, and 3+ part type names, all used repeatedly in the test. (3) commands/user.rs MISSING -- no CREATE USER/ROLE (`CREATE USER regress_unpriv_user`) support (see create_role). (4) `pg_get_functiondef` MISSING -- no ruleutils ddl-deparse ability found for function defs. | self-contained (creates its own temp_func_test schema/objects; no onek/tenk1) |
| create_index | OUT_OF_SCOPE | btree CREATE INDEX (basic, partial, functional/expression, INCLUDE, NULLS [NOT] DISTINCT, CONCURRENTLY), GiST/GIN/hash/SP-GiST index AMs on box/point/polygon/circle/tsvector/array types, REINDEX (TABLE/INDEX/CONCURRENTLY/VERBOSE/SYSTEM), ADD CONSTRAINT ... USING INDEX, partitioned tables (PARTITION BY hash/range) | (1) src/backend/commands/indexcmds.rs STUB -- `DefineIndex` hits `not_yet_reachable` for CONCURRENTLY, expression index columns, explicit opclass, DESC ordering, partial index (WHERE), INCLUDE columns, explicit tablespace, and any non-btree access method -- these are used throughout (lines 56-65 partial idx, 85+ gist, 269+ gin, 366+ hash, 450+ expr idx, 472+ INCLUDE, 495+ CONCURRENTLY). (2) access/gist, access/gin, access/hash, access/spgist MISSING -- whole index AM subsystems absent (only nbtree exists). (3) REINDEX MISSING -- no reindexcmds.rs / Reindex grammar handling found anywhere in src/backend. (4) src/backend/commands/tablecmds.rs STUB -- `PARTITION BY` (`not_yet_reachable("DefineRelation: inheritance")`-adjacent partitioning path) used for `cwi_test ... PARTITION BY hash`. | onek, onek2, tenk1, tenk2, road, ihighway, shighway, point_tbl (+ self-contained tables); note road/ihighway/shighway/onek2 setup itself uses INHERITS, currently not_yet_reachable in tablecmds.rs |
| create_index_spgist | OUT_OF_SCOPE | SP-GiST index AM on point (quad/kd opclasses) and text (radix opclass), KNN ordered scans (`<->`), `^@` starts-with operator, `starts_with()`, INCLUDE columns on SP-GiST | (1) access/spgist MISSING -- no SP-GiST access method translated at all (only nbtree exists under src/backend/access/). (2) src/backend/commands/indexcmds.rs STUB -- `DefineIndex` rejects any non-btree USING clause (`not_yet_reachable("DefineIndex: non-btree access method")`) and INCLUDE columns. | tenk1 (for quad_point_tbl), road (for radix_text_tbl); rest self-contained (CREATE TABLE ... AS SELECT) |
| create_misc | OUT_OF_SCOPE | Multi-level single/multiple table INHERITS (`b_star`/`c_star` inherit `a_star`; `d_star` inherits both `b_star`,`c_star`), wildcard inherited-table queries (`a_star*`), ALTER TABLE ... RENAME COLUMN / ADD COLUMN with `*` inheritance-recursion suffix, TOAST-table creation check, polygon/name/float8/int2 literals | src/backend/commands/tablecmds.rs STUB -- `not_yet_reachable("DefineRelation: inheritance")` fires on the very first `CREATE TABLE b_star (...) INHERITS (a_star)`, so nothing past line 19 can even be set up; INHERITS is the sole and total blocker (PG single-inheritance subsystem, not currently on any translated path). | self-contained (builds its own a_star..f_star hierarchy) |
| create_operator | OUT_OF_SCOPE | CREATE/DROP/COMMENT ON OPERATOR (prefix/infix), operator attributes (COMMUTATOR/NEGATOR/RESTRICT/JOIN/HASHES/MERGES/SORT1/SORT2), custom lexing of `!=`/`<=`/`>=`/`<>` edge cases, USAGE-on-schema/type + EXECUTE-on-function privilege checks for operator creation, shell operators | (1) `CreateOperatorStmt`/`CREATE OPERATOR` grammar rule MISSING -- no match found anywhere in src/backend/parser/gram.lalrpop; the statement cannot even be parsed. (2) commands/operatorcmds.rs MISSING -- no such file exists under src/backend/commands. (3) CREATE ROLE MISSING (used for several USAGE/EXECUTE-privilege sub-tests, see create_role). | self-contained (defines its own path/box/enum-based operators); DO $$ ... $$ block also needs PL/pgSQL execution (also unsupported) |
| create_procedure | OUT_OF_SCOPE | CREATE/ALTER/DROP PROCEDURE, CALL statement (incl. named/default params, OUT/INOUT/VARIADIC, polymorphic anyelement/anyarray, nested CALL), `\df`/`\dfp`/`\dfn` psql meta-commands, ALTER ROUTINE RENAME, GRANT/REVOKE EXECUTE ON PROCEDURE, `pg_get_functiondef` | (1) CALL statement MISSING -- no `CallStmt`/CALL grammar or executor path found in src/backend (grep for CallStmt/"CALL" returns nothing outside CommandTag naming); the entire test is built around CALL. (2) src/backend/commands/functioncmds.rs STUB -- OUT/INOUT parameter modes and parameter DEFAULT hit `not_yet_reachable` in `create_function` (used for nearly every CREATE PROCEDURE in the file). (3) src/backend/utils/fmgr/fmgr.rs STUB -- non-builtin (SQL-language) function/procedure body execution is `unimplemented!`, so even a working CALL couldn't execute the procedure body. (4) commands/user.rs MISSING -- CREATE USER regress_cp_user1 for the privileges section. | self-contained (creates cp_test table + its own procedures) |
| create_role | OUT_OF_SCOPE | CREATE/ALTER/DROP ROLE with attributes (SUPERUSER/CREATEDB/CREATEROLE/REPLICATION/BYPASSRLS/LOGIN/INHERIT/CONNECTION LIMIT/PASSWORD), role membership (IN ROLE/ROLE/ADMIN), GRANT role WITH INHERIT/SET, COMMENT ON ROLE, REASSIGN OWNED BY, SET SESSION AUTHORIZATION, createrole_self_grant GUC, predefined roles (pg_read_all_data etc.) | (1) CREATE ROLE / CREATE USER grammar MISSING -- no `CreateRoleStmt` match anywhere in src/backend/parser/gram.lalrpop. (2) commands/user.rs MISSING -- no such command file exists (only aclchk.rs GRANT/REVOKE machinery exists). (3) catalog/pg_authid.rs STUB -- per its own header comment, "named non-bootstrap roles STAGE" -- only the bootstrap superuser OID is recognized; `aclchk.rs` explicitly `unimplemented!`s on any named role not yet seeded in pg_authid, which every `CREATE ROLE regress_*` in this test would require. | self-contained (all objects are roles/schemas/tables it creates itself) |
| create_schema | OUT_OF_SCOPE | CREATE SCHEMA with AUTHORIZATION (named role and CURRENT_ROLE), nested `schema_element_list` (CREATE TABLE/SEQUENCE/VIEW/INDEX/TRIGGER inside the CREATE SCHEMA), error cases for mismatched schema-qualification, `\d` meta-command, DROP SCHEMA CASCADE, CREATE ROLE for the owning role | (1) src/backend/commands/schemacmds.rs STUB -- `create_schema_command` explicitly `unimplemented!`s when `stmt.schemaElts` is non-empty (every CREATE SCHEMA in this test embeds a nested CREATE TABLE/SEQUENCE/VIEW/INDEX/TRIGGER) and raises ERRCODE_FEATURE_NOT_SUPPORTED for the no-schema-name `AUTHORIZATION role` form (used repeatedly, e.g. line 25). (2) CREATE ROLE MISSING (`CREATE ROLE regress_create_schema_role SUPERUSER` needed before any of the schema statements run) -- see create_role. | self-contained (creates regress_create_schema_role + its own schemas) |

| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| oidjoins | OUT_OF_SCOPE | single anonymous `DO $doblock$ ... END $doblock$;` PL/pgSQL block using `pg_get_catalog_foreign_keys()`, `array_length`, `quote_ident`, dynamic `EXECUTE`, `RAISE NOTICE`/`RAISE WARNING`, verifying FK consistency across all system catalogs | PL/pgSQL execution (DO blocks) -- MISSING: no `DoStmt` handling found anywhere under src/ (grep for `DoStmt` only matches node/parser definitions, not an executor); `pg_get_catalog_foreign_keys()` -- MISSING: not found anywhere in src/backend (introspection function over catalog FK metadata never ported) | self-contained (queries all system catalogs directly) |
| opr_sanity | SETUP_BLOCKED | ~150 SELECT-only sanity queries cross-checking pg_proc/pg_operator/pg_cast/pg_conversion/pg_aggregate/pg_am/pg_amop/pg_amproc/pg_opclass/pg_opfamily/pg_index for dangling/inconsistent entries; relies on a custom SQL-callable C function `binary_coercible(oid,oid)` and `check_sql_fn_retval`-style helpers loaded from the regress test module | test_setup.sql `CREATE FUNCTION binary_coercible(oid, oid) ... AS :'regresslib', 'binary_coercible' LANGUAGE C STRICT STABLE PARALLEL SAFE;` -- MISSING: no `LANGUAGE C` / dynamic shared-library function-loading path exists in src/backend/commands/functioncmds.rs (only SQL-body/catalog-only functions handled); this single fixture function is used in ~40 of the sanity checks in this file, so the whole test is blocked at setup even though the plain catalog SELECTs themselves would mostly run over already-translated pg_proc/pg_operator/pg_am/pg_opclass catalog data | test_setup fixture: custom `binary_coercible` C function (regress.so); reads pg_catalog tables directly |
| partition_aggregate | OUT_OF_SCOPE | `CREATE TABLE ... PARTITION BY LIST/RANGE`, `PARTITION OF ... FOR VALUES IN/FROM/TO`, partitionwise aggregate/join GUCs (`enable_partitionwise_aggregate`, `enable_partitionwise_join`), GROUP BY/HAVING with agg pushdown across partitions, `generate_series`, `to_char`, `EXPLAIN (COSTS OFF)` | commands/tablecmds.rs -- STUB: `not_yet_reachable("DefineRelation: partitioning")` fires on any `PARTITION BY` clause (whole feature absent); partitionwise aggregate/join planner logic (`optimizer/cost.rs` `enable_partitionwise_join`/`enable_partitionwise_aggregate` are declared but partitioning itself doesn't exist so the planner paths are moot) | self-contained (creates pagg_tab etc. itself) |
| partition_info | OUT_OF_SCOPE | `pg_partition_tree()`, `pg_partition_ancestors()`, `pg_partition_root()` introspection functions; `CREATE TABLE ... PARTITION BY RANGE/LIST`, multi-level partitioning, `PARTITION OF ... FOR VALUES FROM (minvalue) TO (...)`, partitioned indexes (`CREATE INDEX ... ON ONLY`, `ALTER INDEX ... ATTACH PARTITION`), legacy inheritance (`INHERITS`), views/matviews interaction | commands/tablecmds.rs -- STUB: `not_yet_reachable("DefineRelation: partitioning")` blocks all `PARTITION BY`/`PARTITION OF` DDL; `pg_partition_tree`/`pg_partition_ancestors`/`pg_partition_root` -- MISSING: none of these functions found anywhere in src/backend | self-contained |
| partition_join | OUT_OF_SCOPE | `CREATE TABLE ... PARTITION BY RANGE`, `PARTITION OF ... FOR VALUES FROM/TO`, `enable_partitionwise_join`, inner/left/right/full joins across partitioned tables, partitionwise join planning, `generate_series`, `to_char`, btree indexes per-partition | commands/tablecmds.rs -- STUB: `not_yet_reachable("DefineRelation: partitioning")` blocks all `PARTITION BY`/`PARTITION OF` DDL (entire file depends on it from line 1) | self-contained |
| partition_prune | OUT_OF_SCOPE | `CREATE TABLE ... PARTITION BY LIST/RANGE/HASH`, deeply nested `PARTITION OF ... FOR VALUES IN/FROM/TO/DEFAULT`, partition pruning at plan and execution time, `PREPARE`/`EXECUTE` with `plan_cache_mode = force_generic_plan`, PL/pgSQL helper functions (`language plpgsql`, `EXECUTE format(...)`), collation-aware pruning | commands/tablecmds.rs -- STUB: `not_yet_reachable("DefineRelation: partitioning")` blocks all `PARTITION BY` DDL used throughout; PL/pgSQL function execution -- MISSING (used for `explain_analyze()` helper and `list_part_fn()`), compounding the partitioning blocker | self-contained |
| password | OUT_OF_SCOPE | `SET password_encryption`/`scram_iterations` GUCs, `CREATE ROLE`/`ALTER ROLE ... PASSWORD`/`RENAME TO`/`DROP ROLE`, MD5 and SCRAM-SHA-256 password hashing and validation, `pg_authid` catalog, `regexp_replace` | commands for role management (CreateRoleStmt/AlterRoleStmt) -- MISSING: no `CreateRoleStmt`/`AlterRoleStmt` handling found anywhere under src/backend/commands (no CREATE ROLE/ALTER ROLE support at all, no pg_authid population path); src/backend/utils/adt/regexp.rs -- MISSING (no regexp module under src/backend/utils/adt; `regexp_replace` used to mask random salts in output) | self-contained (creates/drops regress_passwd* roles itself) |
| path | OUT_OF_SCOPE | `path` geometry type (input/output incl. malformed-input error cases, open `[...]` vs closed `(...)` syntax), `isopen`/`isclosed`/`popen`/`pclose` functions, `pg_input_is_valid`/`pg_input_error_info` non-throwing input API | src/backend/utils/adt/geo.rs -- MISSING (no geometry/path type module under src/backend; only header stub src/utils/geo_decls.rs, not translated: path type input/output, isopen/isclosed/popen/pclose all absent); utils/adt pg_input_is_valid/pg_input_error_info infra -- MISSING (not found anywhere in src/backend) | self-contained |

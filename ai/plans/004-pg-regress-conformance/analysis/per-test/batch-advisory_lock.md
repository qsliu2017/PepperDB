| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| advisory_lock | NEEDS_MODULES | pg_advisory_lock/_shared/_xact/_unlock/_unlock_all/_unlock_shared, pg_locks system view, txn-scoped vs session-scoped lock lifetime, `\gset` | src/backend/utils/adt/lockfuncs.rs -- MISSING (no file; C source `utils/adt/lockfuncs.c` not translated; no `pg_advisory_*` fmgr registrations found anywhere in src/); pg_locks system view -- MISSING (no catalog/view definition found, only internal lock manager structs in storage/lmgr/lock.rs) | self-contained (uses `pg_database`/`pg_locks` catalogs only) |
| aggregates | NEEDS_MODULES | avg/sum/max/min/count, stddev_pop/samp, var_pop/samp, regr_* (SQL2003 binary aggregates), covar_pop/samp, corr, any_value, string_agg, array_agg, percentile_cont/disc (ordered-set aggs), mode, user-defined CREATE AGGREGATE (oldstyle sfunc1/basetype), numeric/float8 arithmetic, COPY FROM data file, row()-valued min/max | src/backend/utils/adt/float.rs -- STUB (no stddev_pop/var_pop/regr_*/covar_*/corr/float8_accum transition fns found); src/backend/utils/adt/numeric.rs -- STUB (no numeric_avg/numeric stddev/var transition fns, though scalar numeric ops mostly done); src/backend/utils/adt/varlena.rs -- STUB (no string_agg transition fns); array_agg -- MISSING (depends on array subsystem, see arrays.sql finding); src/commands/defrem.rs `DefineAggregate` -- STUB (unimplemented!, blocks CREATE AGGREGATE alt_agg-style tests used later in file, and ordered-set/percentile support in executor) | onek, student (ONLY student), aggtest (local), regr_test (local), bitwise_test etc (local) |
| alter_generic | NEEDS_MODULES | ALTER {FUNCTION,AGGREGATE,OPERATOR,OPERATOR CLASS/FAMILY,CONVERSION,LANGUAGE,FOREIGN DATA WRAPPER,SERVER,TEXT SEARCH CONFIGURATION/DICTIONARY/PARSER/TEMPLATE,STATISTICS} RENAME/OWNER/SET SCHEMA, CREATE USER/ROLE with membership, GRANT ON SCHEMA, CREATE AGGREGATE (oldstyle) | src/commands/user.rs `CreateRole`/`AlterRole` -- STUB (unimplemented!, blocks CREATE USER ... IN ROLE); src/commands/defrem.rs `DefineAggregate`/`AlterFunction`/`DefineOpClass`/`DefineOpFamily`/`CreateForeignDataWrapper`/`DefineTSParser`/`DefineTSDictionary`/`DefineTSTemplate`/`DefineTSConfiguration` -- all STUB (unimplemented!); src/commands/conversioncmds.rs `CreateConversionCommand` -- STUB (unimplemented!); AlterOperator -- STUB (see alter_operator finding) | self-contained (creates its own schemas/roles/objects) |
| alter_operator | NEEDS_MODULES | CREATE OPERATOR with COMMUTATOR/NEGATOR/RESTRICT/JOIN/HASHES/MERGES, ALTER OPERATOR SET (RESTRICT/JOIN/COMMUTATOR/NEGATOR/MERGES/HASHES), CREATE USER + SESSION AUTHORIZATION (owner perm check), pg_depend/pg_describe_object introspection | src/commands/defrem.rs `DefineOperator`/`AlterOperator`/`RemoveOperatorById` -- STUB (unimplemented!, `/* commands/operatorcmds.c */` section untranslated); src/commands/user.rs `CreateRole` -- STUB (unimplemented!, blocks `CREATE USER regress_alter_op_user`) | self-contained |
| alter_table | NEEDS_MODULES | ALTER TABLE ADD/DROP COLUMN (supported), but also RENAME COLUMN/TABLE, OWNER TO, ALTER COLUMN TYPE, ATTACH/DETACH PARTITION, INHERIT/NO INHERIT, SET STORAGE/TABLESPACE, CLUSTER ON, identity columns, generated columns, validate constraint, USING index-am, partitioning DDL broadly | src/backend/commands/tablecmds.rs `ata_exec_cmd` -- STUB for everything except AddColumn/DropColumn/ColumnDefault/SetNotNull/DropNotNull/AddConstraint/DropConstraint (`other => not_yet_reachable`, line ~582); `DefineRelation` -- STUB for inheritance/partitioning/OF type/WITH storage/tablespace/USING am (multiple `not_yet_reachable` calls lines 70-113) | self-contained (creates its own tables: attmp, anothertab, etc.) plus touches `stud_emp`/`pg_class` from test_setup-adjacent fixtures |
| amutils | OUT_OF_SCOPE | pg_indexam_has_property/pg_index_has_property/pg_index_column_has_property against btree, gist (gcircleind), hash (hash_i4_index), spgist (sp_radix_ind/sp_quad_ind), gin (botharrayidx), brin (brinidx) index AMs | access/gist, access/hash, access/spgist, access/gin, access/brin -- all MISSING (only access/nbtree exists per project notes); src/backend/utils/adt/amutils.rs -- MISSING (no file; C `utils/adt/amutils.c` not translated, no `pg_indexam_has_property`-family fmgr registrations found) | requires index objects created by other regress scripts (create_index, create_index_spgist, hash_index, brin) that use non-btree AMs -- not part of test_setup |
| arrays | OUT_OF_SCOPE | array types (int2[]/int4[][][]/text[][]/float8[]/char(5)[]/varchar(5)[]), array literal I/O, multi-dim slice assignment/subscripting (a[1:5], b[1:1][1:2][1:2]), ARRAY[] constructor, array operators/functions (array_dims, array_fill, array_agg via array_userfuncs), COPY FROM data file | src/utils/array.rs -- STUB (548 lines, 46 unimplemented!/not_yet_reachable -- header-only skeleton, no real array datum encode/decode); src/utils/arrayaccess.rs -- STUB; src/backend/utils/adt/arrayfuncs.rs and array_userfuncs.rs -- MISSING (no such files exist; array I/O, subscripting exec support, array_agg, array_dims/array_fill absent) | array_op_test (local, via COPY from data file) |
| async | NEEDS_MODULES | pg_notify(), NOTIFY/LISTEN/UNLISTEN/UNLISTEN *, pg_notification_queue_usage() | src/commands/async.rs -- MISSING real impl (79-line file, every function body is `unimplemented!()`: async_notify, async_listen, async_unlisten, async_unlisten_all, notify_my_front_end, pre/at_commit_notify, etc.); no `pg_notify`/`pg_notification_queue_usage` fmgr bindings found anywhere in src/ | self-contained |

| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| numeric | NEEDS_MODULES | `numeric(p,s)` arithmetic (add/sub/mul/div/mod), `sqrt`/`ln`/`log`/`power`/`exp`/`factorial`, `abs`/`ceil`/`floor`/`round`/`trunc`/`sign`/`div`/`gcd`/`lcm`/`scale`/`min_scale`/`trim_scale`, `to_char`/`to_number`, `width_bucket(numeric,...)`, `variance`/`stddev`/`var_pop`/`var_samp`/`stddev_pop`/`stddev_samp` aggregates, `generate_series(numeric,numeric[,numeric])`, `pg_input_is_valid`/`pg_input_error_info` for numeric, `^` power operator, `'NaN'`/`'Inf'` numeric literals | 1) `src/backend/utils/adt/numeric.rs` -- STUB: `numeric_sqrt`, `numeric_exp`, `numeric_ln`, `numeric_log`, `numeric_power`, `numeric_fac` (factorial) are all `unimplemented!()` ("transcendental, not yet reachable"); the variance/stddev aggregate family (`numeric_var_samp`, `numeric_var_pop`, `numeric_stddev_samp`, `numeric_stddev_pop`, `numeric_avg`, `numeric_accum`, and the `poly_*` variants) are all routed through an `agg_stub!()` macro that is unimplemented. 2) `width_bucket` for numeric -- MISSING: no `width_bucket`/`numeric_width_bucket` function exists anywhere under `src/backend/utils/adt`. 3) `generate_series(numeric, numeric[, numeric])` -- MISSING: `generate_series` only exists for int4 (`int.rs`) and timestamp/timestamptz (`timestamp.rs`); no numeric overload. 4) `src/backend/parser/scan.rs` -- STUB: the lexer has no `^` operator token and no hex/underscore support needed elsewhere in the file, but more importantly no `DO`-block/dollar-quote issue here; however `^` (used for `10::numeric ^ 131071` power expressions) is not tokenized at all (only `+ - * / % < > = <= >= <> !=` exist), so any `a ^ b` power expression fails to parse. 5) `pg_input_is_valid`/`pg_input_error_info` -- MISSING: no such functions exist anywhere in `src/` (needed for the numeric input-validation section of the test). | self-contained (creates num_data/num_exp_*/num_result tables) |

| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| namespace | NEEDS_MODULES | `CREATE SCHEMA name CREATE ... CREATE ... CREATE ...` (nested schema-element list: index/view/table in one statement); `ALTER SCHEMA ... RENAME`; `CREATE SCHEMA IF NOT EXISTS`; `SET search_path`; plpgsql function; `CREATE MATERIALIZED VIEW` + `REFRESH MATERIALIZED VIEW`; `REINDEX TABLE`; `CLUSTER ... USING`; `ANALYZE`/`VACUUM FULL` on schema-qualified table | 1) `src/backend/commands/schemacmds.rs` -- STUB: `CreateSchemaCommand` hits `unimplemented!("CreateSchemaCommand: schema_element_list ...")` for the nested `CREATE SCHEMA ... CREATE TABLE/VIEW/INDEX` form used at the very top of the test. 2) `src/commands/matview.rs` (materialized views) -- STUB: `ExecRefreshMatView`/`RefreshMatViewByOid`/`SetMatViewPopulatedState` are all `unimplemented!()`, and there is no `matviewcmds.rs`/`CreateMatViewStmt` handler under `src/backend/commands`, so `CREATE MATERIALIZED VIEW` / `REFRESH MATERIALIZED VIEW` cannot run. 3) REINDEX -- MISSING: no `ReindexStmt`/reindex command implementation found under `src/backend/commands`, needed for `REINDEX TABLE test_maint_search_path.test_maint`. | self-contained (creates its own schemas/tables) |

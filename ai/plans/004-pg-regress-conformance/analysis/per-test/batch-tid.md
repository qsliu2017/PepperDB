| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| tid | NEEDS_MODULES | tid literal cast/IO, `pg_input_is_valid`/`pg_input_error_info`, `min(ctid)`/`max(ctid)` aggregate, `currtid2()`, materialized views, sequences, views over ctid | `src/backend/utils/adt/tid.rs` MISSING (no tid type module: tidin/tidout/tid comparison ops needed for cast, `<`/`>`/min/max); `src/backend/utils/fmgr/fmgr.rs` STUB (`InputFunctionCallSafe`/`DirectInputFunctionCallSafe` unimplemented, blocks `pg_input_is_valid`/`pg_input_error_info`); `currtid2` MISSING (no hits anywhere in src/backend) | self-contained |
| tidrangescan | NEEDS_MODULES | `SET enable_seqscan`, CREATE TABLE, ctid range predicates (`<`,`<=`,`>`,`>=`) planned as TID Range Scan, `EXPLAIN (COSTS OFF)`, VACUUM, LATERAL rescans, SCROLL CURSOR + FETCH NEXT/PRIOR/FIRST/LAST | `src/backend/commands/explain.rs` MISSING (no EXPLAIN statement handler at all; every query here is preceded by EXPLAIN); `access/nbtree`-style TID range scan plan/exec node MISSING (no `nodeTidrangescan.rs` under src/backend/executor -- only a 14-line stub exists at src/executor/nodeTidrangescan.rs); `src/backend/utils/adt/tid.rs` MISSING (ctid comparison operators `<`,`<=`,`>`,`>=` needed for range quals) | self-contained |
| tidscan | NEEDS_MODULES | ctid equality scan, OR'd/ANY/!=ANY ctid quals, sub-AND ctid extraction, nestloop-with-inner-tidscan joins, cursors FETCH BACKWARD/FIRST, `WHERE CURRENT OF`, `EXPLAIN (ANALYZE,...)`, bulk ctid joins on tenk1, SERIALIZABLE + `pg_locks`/SIReadLock check | `src/backend/executor/nodeTidscan.rs` STUB (`TidListEval` at line 149 is `unimplemented!("TidListEval: ctid-qual TID extraction not yet reachable for this milestone")` -- this is the core ctid-qual-to-TID-list extraction all tidscan queries need); `src/backend/commands/explain.rs` MISSING (EXPLAIN handler absent, blocks all EXPLAIN lines including EXPLAIN ANALYZE); `WHERE CURRENT OF` execution MISSING (no execCurrent.rs / CurrentOfExpr executor support, only parser/optimizer plumbing exists); predicate locking/SSI (`SIReadLock` via `pg_locks`) MISSING (only comment-level references, no real implementation found) | tenk1 (bulk ctid join queries) + self-contained (tidscan table) |
| time | NEEDS_MODULES | `time(n)` type, time literal parsing incl. TZ-ignored and rounding edge cases, comparison ops, `EXTRACT`/`date_part` (MICROSECOND/MILLISECOND/SECOND/MINUTE/HOUR/EPOCH + error cases), `pg_input_is_valid`/`pg_input_error_info` for `time` | `src/backend/utils/fmgr/fmgr.rs` STUB (`InputFunctionCallSafe` unimplemented blocks the two `pg_input_is_valid`/`pg_input_error_info` queries) -- the base `time` type itself (TIME_TBL insert/select/compare, EXTRACT, date_part) appears to be implemented in datetime.rs/date.rs (time-of-day helpers) with no unimplemented! hit for these paths | self-contained |
| timestamp | NEEDS_MODULES | `timestamp(n)`, shorthand literals (today/yesterday/tomorrow/now/epoch/infinity), `pg_sleep`, wide literal-format parsing, `pg_input_is_valid`/`pg_input_error_info`, boundary overflow, `date_trunc`, `date_bin` (incl. BC/overflow), `date_part`/`extract` incl. julian/epoch, `to_char` (DAY/Month/RM/FF/etc.), `make_timestamp`, `generate_series(timestamp,timestamp,interval)`, infinite-timestamp arithmetic, `age()` | `src/backend/utils/adt/timestamp.rs` STUB on `generate_series_timestamp`/`generate_series_timestamp_support` (line 2884+, `unimplemented!("...needs the SRF ValuePerCall FuncCallContext")`) -- blocks all `generate_series(timestamp,...)` queries; `pg_sleep` MISSING (no hits anywhere in src) -- blocks the "now changes over time" tests; `src/backend/utils/fmgr/fmgr.rs` STUB (`InputFunctionCallSafe`) blocks `pg_input_is_valid`/`pg_input_error_info`; core arithmetic/date_trunc/date_bin/date_part/to_char/make_timestamp bodies otherwise appear FULL (no unimplemented! hit on those specific paths) | self-contained |
| timestamptz | NEEDS_MODULES / OUT_OF_SCOPE (mixed) | everything in `timestamp` plus named IANA timezone handling (`AT TIME ZONE 'America/New_York'`, `SET TimeZone = 'Europe/...'`, dynamic-abbreviation DST tests MSK/Moscow, `Europe/Helsinki`, `make_timestamptz(..., 'Europe/Prague')`), `date_trunc(..., zone)`, `date_bin`, `to_char` OF/TZH/TZM, `generate_series`, `AT LOCAL`, `pg_input_is_valid` | IANA timezone database is unported entirely (`src/backend/utils/adt/datetime.rs` STAGED comment at line 25-26 "STAGED (needs the unported IANA timezone database / session GUCs)"; `timestamptz_zone`/`timestamp_zone`/`timestamptz_trunc_zone`/`make_timestamptz_at_timezone`/`timestamptz_pl_interval_at_zone` all `unimplemented!` in timestamp.rs) -- this is a large subsystem (pg_tzset/tzdata) so treat as OUT_OF_SCOPE for the named-zone majority of the file; additionally `generate_series_timestamptz`/`generate_series_timestamptz_at_zone` STUB (unimplemented!, timestamp.rs ~2887-2891); `pg_sleep` MISSING; `src/backend/commands/explain.rs` MISSING (blocks the one EXPLAIN query on index use); `src/backend/utils/fmgr/fmgr.rs` STUB blocks `pg_input_is_valid` | self-contained |
| timetz | NEEDS_MODULES | `time(n) with time zone` type, fixed-offset literal parsing/rounding edge cases, comparison ops, `EXTRACT`/`date_part` incl. TIMEZONE/TIMEZONE_HOUR/TIMEZONE_MINUTE, `pg_input_is_valid`/`pg_input_error_info`, `timetz_zone`/`timetz_izone`/`AT LOCAL`/`AT TIME ZONE` (named + interval), views over timetz | `src/backend/utils/adt/date.rs` STUB (`timetz_zone` at line 2121 is `unimplemented!`/stub with `_fcinfo` unused param -- named-zone `AT TIME ZONE 'UTC+10'` needs IANA tz DB per datetime.rs's STAGED comment); `src/backend/utils/fmgr/fmgr.rs` STUB blocks `pg_input_is_valid`/`pg_input_error_info`; `timetz_izone` (interval-based AT TIME ZONE) appears implemented | self-contained |
| transactions | NEEDS_MODULES | BEGIN/COMMIT/ABORT/ROLLBACK, SAVEPOINT/RELEASE/ROLLBACK TO, nested subtransactions, transaction characteristics (ISOLATION LEVEL/READ ONLY/DEFERRABLE), SET SESSION CHARACTERISTICS, COMMIT/ROLLBACK AND CHAIN, cursors across subtransaction abort, `CREATE FUNCTION ... LANGUAGE SQL` and `LANGUAGE plpgsql` bodies (STABLE/VOLATILE semantics), multi-statement Query-message (`\;`-separated) implicit transaction blocks, `SET TRANSACTION SNAPSHOT` | PL/pgSQL execution engine MISSING entirely (no plpgsql/pl_exec module found; blocks `max_xacttest`, `inverse`, `invert`, `create_temp_tab` bodies) -- OUT_OF_SCOPE-sized; `LANGUAGE SQL` function BODY execution also not wired (no fmgr_sql/CallSQLFunction hits) so even the non-plpgsql `max_xacttest`/`errfunc` variants can't run; `src/backend/tcop/postgres.rs` STUB (`exec_simple_query` line ~463 `unimplemented!("exec_simple_query: multi-statement query strings deferred")` -- blocks the entire final third of the file, the `SELECT 1\; SELECT 2\;`-style implicit transaction block tests). Core BEGIN/COMMIT/SAVEPOINT/subtransaction/CHAIN machinery in `src/backend/access/transam/xact.rs` is otherwise FULL. | tenk1 (cursor test at line 277), int8_tbl (cursor test at line 427) |

| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| numerology | NEEDS_MODULES | Non-decimal integer literals (`0b`/`0o`/`0x`), underscore-separated numeric literals (`1_000_000`), numeric-literal error cases, `PREPARE`/`$n` params, `DO $$ ... FOR i IN a..b LOOP ... $$` plpgsql loop, implicit/explicit type conversions (`float8()`/`int4()`/`int2()`), `GROUP BY` combinations over `INT4_TBL`/`FLOAT8_TBL`/`INT2_TBL` | `src/backend/parser/scan.rs` -- STUB: the lexer's integer/float regexes only match plain decimal digits (`[0-9]+` and simple float forms); there is no `0b`/`0o`/`0x` prefix handling and no underscore-separator support, so essentially the entire first two-thirds of the file (all the numeric-literal and error-case SELECTs) fails to lex. It also has no dollar-quote token, so the `DO $$ ... $$` loop block cannot be parsed either. | `INT4_TBL`, `INT2_TBL`, `FLOAT8_TBL` (from test_setup.sql) |

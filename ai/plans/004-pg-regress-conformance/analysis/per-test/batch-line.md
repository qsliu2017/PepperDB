| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| line | OUT_OF_SCOPE | CREATE TABLE with `line` column, LINE literal parsing (`{A,B,C}` / two-point / bad-value error cases), `line(point,point)` constructor, `=` operator, `pg_input_is_valid`/`pg_input_error_info` | src/backend/utils/adt/geo.rs -- MISSING (only header struct/macro stub src/utils/geo_decls.rs exists, e.g. `DatumGetLineP`/`LinePGetDatum`; no line input/output/constructor/operator functions, and `line` is not registered in the type catalog at all); pg_input_is_valid/pg_input_error_info -- MISSING (not found anywhere in src) | self-contained |
| lock | NEEDS_MODULES | `LOCK [TABLE] ... IN <mode> MODE [NOWAIT]`, views/nested views lock cascading, `CREATE ROLE`/`SET ROLE`/GRANT-based lock permission checks, table inheritance (`INHERITS`), `pg_locks`/`pg_class` system-view introspection, `CREATE FUNCTION ... LANGUAGE C` calling a test C library (`test_atomic_ops`) | src/commands/lockcmds.rs -- STUB (`LockTableCommand` is `unimplemented!()`, and `Node::LockStmt` is not even matched in src/backend/tcop/utility.rs's statement dispatcher); src/commands/user.rs -- STUB (`CREATE ROLE` / role management all `unimplemented!()`); table inheritance (`INHERITS`) -- MISSING (no handling of `inhRelations` in src/commands/tablecmds.rs); pg_locks system view -- MISSING (only a TODO comment in src/backend/storage/lmgr/lock.rs, "TODO(lockfuncs): the pg_locks snapshot"); dynamic C-language function loading (`LANGUAGE C` + dlopen of test .so) -- OUT_OF_SCOPE (no dynamic-library FFI loader for user extension libs) | self-contained |
| lseg | OUT_OF_SCOPE | CREATE TABLE with `lseg` column, LSEG literal parsing (bracketed/point-pair/comma forms, bad-value error cases), `lseg(point,point)` constructor, `pg_input_is_valid`/`pg_input_error_info` | src/backend/utils/adt/geo.rs -- MISSING (only header struct stub src/utils/geo_decls.rs with `LSEG` struct + `DatumGetLsegP`/`LsegPGetDatum`; no lseg input/output/constructor functions, no catalog registration of the `lseg` type); pg_input_is_valid/pg_input_error_info -- MISSING | self-contained |
| macaddr | NEEDS_MODULES | CREATE TABLE with `macaddr` column, macaddr literal parsing (multiple textual formats incl. invalid cases), comparison ops `< > <= >= = <>`, bitwise `~ & \|`, `trunc()`, `CREATE INDEX USING btree`/`USING hash`, `pg_input_is_valid`/`pg_input_error_info` | src/backend/utils/adt/mac.rs -- MISSING (only header struct stub src/utils/inet.rs defines the `macaddr` struct layout + `DatumGetMacaddrP`/`MacaddrPGetDatum`; no macaddr input/output/comparison/bitwise/trunc functions, no type-catalog registration); src/access/hash.rs -- STUB (hash index AM has 68 `unimplemented!()` call sites, e.g. `hashbuild`/`hashinsert`/`hashbeginscan` all stubbed, blocking `CREATE INDEX ... USING hash`); pg_input_is_valid/pg_input_error_info -- MISSING | self-contained |
| macaddr8 | NEEDS_MODULES | macaddr8 literal parsing/casts (incl. EUI-48->EUI-64 forms, invalid cases), `macaddr8_set7bit()`, comparison ops, bitwise `~ & \|`, `trunc()`, cast macaddr8<->macaddr, `CREATE INDEX USING btree`/`USING hash`, `pg_input_is_valid`/`pg_input_error_info` | src/backend/utils/adt/mac8.rs -- MISSING (only header struct stub src/utils/inet.rs defines `macaddr8` layout + `DatumGetMacaddr8P`/`Macaddr8PGetDatum`; no macaddr8 input/output/comparison/bitwise/trunc/set7bit functions, no type-catalog registration); src/access/hash.rs -- STUB (hash index AM stubbed, blocking `CREATE INDEX ... USING hash`); pg_input_is_valid/pg_input_error_info -- MISSING | self-contained |
| maintain_every | OUT_OF_SCOPE | `CREATE ROLE`/`SET ROLE`, `CREATE TEMP TABLE` with `ON COMMIT DELETE ROWS`, table inheritance (`INHERITS`), expression index `((1))`, `ANALYZE <table>` and bare database-wide `ANALYZE`, `pg_class.reltuples`/`relhassubclass` introspection | src/commands/user.rs -- STUB (`CREATE ROLE` all `unimplemented!()`); table inheritance (`INHERITS`) -- MISSING (no handling in src/commands/tablecmds.rs, needed for `past_inh_db_child INHERITS (past_inh_db_parent)` and the `relhassubclass` check) -- note bare `ANALYZE` (database-wide, via `get_all_vacuum_rels`) and per-table `ANALYZE` are already implemented in src/backend/commands/vacuum.rs, not blockers | self-contained |
| matview | NEEDS_MODULES | `CREATE MATERIALIZED VIEW ... [WITH [NO] DATA]`, `REFRESH MATERIALIZED VIEW [CONCURRENTLY]`, `\d+` on matviews, `ALTER MATERIALIZED VIEW ... SET SCHEMA`, unique-index-based concurrent refresh diffing, `pg_class.relispopulated`, EXPLAIN on CREATE MATVIEW, `box` type (mvtest_boxes), `fipshash()`, roles/GRANT/DEFAULT PRIVILEGES, PL/pgSQL functions creating matviews via SPI | src/commands/matview.rs -- STUB (`SetMatViewPopulatedState`/`ExecRefreshMatView`/`RefreshMatViewByOid`/`CreateTransientRelDestReceiver`/`MatViewIncrementalMaintenanceIsEnabled` all `unimplemented!()`); `CreateTableAsStmt`/matview-CREATE and `RefreshMatViewStmt`/`Node::LockStmt`-adjacent dispatch -- MISSING (not matched in src/backend/tcop/utility.rs statement dispatcher, so CREATE MATERIALIZED VIEW cannot even be routed to matview.rs); src/backend/utils/adt/geo.rs -- MISSING (box type used by mvtest_boxes, no input/output/equality); src/commands/user.rs -- STUB (CREATE ROLE, used for regress_matview_user/regress_user_mvtest); PL/pgSQL body execution -- OUT_OF_SCOPE (mvtest_func/mvtest_drop_the_index use `LANGUAGE plpgsql` bodies, not executed); fipshash() -- MISSING (not found anywhere in src) | self-contained |
| md5 | NEEDS_MODULES | `md5(text)`, `md5(bytea)` SQL functions, equality against literal hex-string results | src/common/md5.rs -- STUB (`pg_md5_hash`/`pg_md5_binary`/`pg_md5_encrypt` are all `unimplemented!()`); SQL-callable `md5_text`/`md5_bytea` fmgr bindings -- MISSING (no fmgr registration of an `md5` builtin function anywhere in src, so even fixing the stub would not make `md5()` callable from SQL) | self-contained |

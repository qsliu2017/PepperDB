| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| copy2 | NEEDS_MODULES | COPY FROM/TO STDIN/STDOUT (text+CSV), COPY option set (freeze/header/quote/escape/force_quote/force_not_null/force_null/on_error/reject_limit/log_verbosity/encoding/convert_selectively/DEFAULT), COPY ... WHERE, plpgsql BEFORE/AFTER INSERT triggers, ROW LEVEL SECURITY (CREATE POLICY/ENABLE/FORCE ROW LEVEL SECURITY, column-level GRANT), domain NOT NULL via COPY error path, savepoint/subxact visibility of COPY-inserted rows, INSTEAD OF INSERT trigger on view w/ COPY optimization | `src/backend/commands/copy.rs` (STUB: `do_copy` errors on `stmt.filename.is_none()` i.e. STDIN/STDOUT -- "COPY to/from STDIN/STDOUT is not yet supported"; also BINARY format staged) -- **dominant blocker**; `src/backend/commands/copy.rs` `process_copy_options` (STUB: only format/delimiter/null/header/quote/escape recognized, all of freeze/force_quote/force_not_null/force_null/on_error/reject_limit/log_verbosity/encoding/convert_selectively/default fall to the `other =>` "option not recognized" error path); PL/pgSQL body execution (MISSING: no plpgsql module in `src/`; `fn_x_before`/`fn_x_after` triggers need real body execution, not just catalog registration); Row-Level Security (MISSING: no CREATE POLICY / ENABLE ROW LEVEL SECURITY handling found anywhere in `src/backend`) | self-contained (creates its own tables `x`, `y`, `testnl`, `testeoc`, `testnull`, `vistest`, `forcetest`, `check_con_tbl`, `rls_t1`, `instead_of_insert_tbl(_view[_2])`, `check_ign_err[2]`, `hard_err`, `copy_default`) |
| copydml | NEEDS_MODULES | COPY (INSERT/UPDATE/DELETE ... RETURNING) TO stdout, `\copy` meta-command, CREATE RULE (DO INSTEAD/ALSO, conditional WHERE, NOTIFY), plpgsql BEFORE/AFTER trigger firing raising NOTICE with TG_OP/TG_WHEN | `src/backend/commands/copy.rs` `do_copy` (STUB: STDOUT not supported, same as copy2) -- dominant blocker; `src/backend/commands/copyto.rs` `copy_to_query` (STUB: routes the query through `standard_planner`, the SELECT-only planner -- no support for planning/running an INSERT/UPDATE/DELETE...RETURNING query) -- needed even if STDOUT lands; `src/backend/rewrite/rewriteDefine.rs`/`rewriteHandler.rs` (STUB: `not_yet_reachable("DefineQueryRewrite: non-SELECT (INSERT/UPDATE/DELETE) rule firing")` and `not_yet_reachable("RewriteQuery: INSERT/UPDATE/DELETE/MERGE rule firing (M11)")` -- CREATE RULE ON INSERT/UPDATE/DELETE is unimplemented, only ON SELECT view rules work); PL/pgSQL body execution (MISSING, for `qqq_trig()` firing) | self-contained (`copydml_test` table) |
| copyencoding | NEEDS_MODULES | COPY (SELECT ...) TO file WITH ENCODING option, COPY FROM file WITH ENCODING option, SET/RESET client_encoding, getdatabaseencoding() | `src/backend/commands/copy.rs` `process_copy_options` (STUB: "encoding" option name is not in the recognized set -- falls to `other => copy_error(... "option ... not recognized")`); note this test uses a server file path (`:'utf8_csv'`, an absolute path via psql variable) not STDIN/STDOUT, so the COPY-wire blocker does NOT apply here -- the sole blocker is the missing ENCODING option (and any multibyte-encoding conversion machinery it implies) | self-contained (creates `copy_encoding_tab`); depends on `getdatabaseencoding() = 'UTF8'` (test self-skips otherwise) |
| copyselect | NEEDS_MODULES | COPY table/(SELECT ...)/(SELECT ... FOR UPDATE)/(SELECT ... UNION ...)/(join) TO STDOUT, COPY error cases (view direct COPY, SELECT INTO inside COPY, column list on subselect), `\copy`, psql multi-command `\;` COPY sequencing | `src/backend/commands/copy.rs` `do_copy` (STUB: STDOUT not supported) -- sole/dominant blocker; once STDOUT lands, the SELECT-shaped `copy_to_query` path already uses `standard_planner` which is fine for plain SELECT/UNION/JOIN/FOR UPDATE queries exercised here | self-contained (`test1`, `test2`, `v_test1`, `test3`) |
| create_aggregate | NEEDS_MODULES | CREATE AGGREGATE (old + new syntax: sfunc/stype/basetype/initcond, zero-arg, "any", multi-arg, DISTINCT/strict-sensitive, ordered-set aggs with built-in support funcs, moving-aggregate mstype/msfunc/minvfunc, serialfunc/deserialfunc/combinefunc, finalfunc_modify), CREATE OR REPLACE AGGREGATE, ALTER AGGREGATE RENAME, COMMENT ON AGGREGATE, DROP AGGREGATE, anycompatible/variadic polymorphism, pg_aggregate catalog introspection | No `aggregatecmds.rs`/equivalent module (MISSING: `src/backend/commands/` has no aggregate command file at all); `src/backend/tcop/utility.rs` `Node::DefineStmt` dispatch only handles `ObjectType::TYPE` and `ObjectType::COLLATION`, so `ObjectType::AGGREGATE` falls to `other => not_yet_reachable("ProcessUtility: DefineStmt {other:?}")` -- CREATE AGGREGATE cannot be executed at all; ALTER AGGREGATE / DROP AGGREGATE / COMMENT ON AGGREGATE likely hit similar unhandled dispatch (not individually verified but no supporting command module exists) | self-contained (creates its own functions/types: `aggtype`, `sum3`, `sum2`, `myavg`, etc.) |
| create_am | OUT_OF_SCOPE | CREATE ACCESS METHOD ... TYPE INDEX/TABLE HANDLER, CREATE OPERATOR CLASS USING gist2, GIST index creation/scan/EXPLAIN, DROP ACCESS METHOD CASCADE, default_table_access_method GUC, ALTER TABLE/MATERIALIZED VIEW SET ACCESS METHOD, table AM pluggability (heap2 alias), CREATE TABLE ... PARTITION BY ... USING, pg_am/pg_depend introspection, foreign data wrapper interaction | `access/gist` (MISSING: `find src/backend/access` shows only `transam, rmgrdesc, nbtree, common, heap, table, index` -- no gist/gin/spgist/brin/hash at all); `CreateAmStmt` (MISSING: no grammar rule for `CREATE ACCESS METHOD` in `gram.lalrpop`, no handler in `tcop/utility.rs`); table AM pluggability layer + partitioning (`PARTITION BY`) are also unimplemented per project scope notes | self-contained aside from depending on `fast_emp4000`/`grect2ind`/box type created by the earlier `create_index.sql` test in the same schedule group (not a `test_setup.sql` fixture) |
| create_cast | NEEDS_MODULES | CREATE TYPE (shell + I/O funcs), CREATE CAST ... WITHOUT FUNCTION [AS IMPLICIT], CREATE CAST ... WITH INOUT, CREATE CAST ... WITH FUNCTION ... [AS IMPLICIT], DROP CAST, pg_cast/pg_depend introspection | `CreateCastStmt` (MISSING: no grammar rule for `CREATE CAST`/`DROP CAST` found in `gram.lalrpop`; no handler in `tcop/utility.rs`; no `pg_cast` command module under `src/backend/commands`) -- the whole CREATE CAST feature is absent, though the underlying shell-type + C-language I/O function machinery it depends on (`CREATE TYPE`, `CREATE FUNCTION ... LANGUAGE internal`) is otherwise supported | self-contained (creates `casttesttype` and its functions) |
| create_function_c | OUT_OF_SCOPE | LOAD command (dynamic shared-library loading), CREATE FUNCTION ... LANGUAGE C AS 'file','symbol' (error-path only: nonexistent file/symbol), CREATE FUNCTION ... LANGUAGE internal AS 'nosuch' (error path), regexp_replace on error message, \gset/\getenv psql meta-commands | `LoadStmt` (MISSING: no grammar rule for bare `LOAD 'libname'` found, no handler in `tcop/utility.rs`); `src/backend/commands/functioncmds.rs` `create_function` (STUB: `prolang` match only recognizes `"internal"` and `"sql"`; `"c"` falls to the `other =>` arm which raises `language "..." does not exist` -- wrong error entirely, since C is a real, just call-time-unloadable, language) -- this test is fundamentally about C-language dynamic loading (`dlopen`/`.so` loading, `PG_FUNCTION_INFO_V1` symbol resolution), a whole subsystem (in-process native extension loading) not applicable to PepperDB's Rust architecture | self-contained (relies on the `regress.so` test library built from `ref/postgres/src/test/regress/regress.c`, which has no PepperDB equivalent) |

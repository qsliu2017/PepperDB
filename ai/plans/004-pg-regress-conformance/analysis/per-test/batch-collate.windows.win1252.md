| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| collate.windows.win1252 | OUT_OF_SCOPE | CREATE SCHEMA/TABLE with COLLATE clauses (en_US/sv_SE/ja_JP.eucjp/C), CREATE DOMAIN with COLLATE, ORDER BY ... COLLATE, WIN1252 client_encoding, self-skips unless Windows+WIN1252 | `pg_import_system_collations`: MISSING (no hit anywhere in src/backend) -- needed to populate pg_collation with en_US/sv_SE; non-C libc/ICU collation support: MISSING (src/backend/utils/adt/pg_locale.rs / equivalent not found) -- needed for locale-aware `>=`/ORDER BY comparisons; Windows-only locale integration is a whole untranslated subsystem | self-contained |
| combocid | READY | temp table, multi-statement txn with dummy INSERT SELECT LIMIT 0, cmin/cmax via ctid,cmin SELECT, SAVEPOINT/ROLLBACK TO, DECLARE CURSOR + FETCH ALL, SELECT ... FOR UPDATE, numeric column type, second txn with UPDATE/SAVEPOINT/ROLLBACK TO combo | none -- combo cid mechanism implemented in src/backend/utils/time/combocid.rs (HeapTupleHeaderGetCmin/Cmax real bodies, used by heapam_visibility.rs); numeric_add/sub registered in fmgr_builtins (src/backend/utils/adt/numeric.rs); SAVEPOINT/ROLLBACK TO in src/backend/access/transam/xact.rs; cursors in src/backend/commands/portalcmds.rs | self-contained |
| comments | READY | single-line `--` and block `/* */` SQL comments, including nested block comments and comments embedded inside string-like text, no actual relations | none -- pure lexer/scanner feature (src/backend/parser/scan.rs) | self-contained |
| compression | OUT_OF_SCOPE | CREATE TABLE ... COMPRESSION pglz/lz4, `pg_column_compression()`, LIKE INCLUDING COMPRESSION, ALTER COLUMN SET COMPRESSION/SET STORAGE, TOAST external storage + compressed slicing (SUBSTR), materialized views, PARTITION BY HASH + ATTACH PARTITION, INHERITS | TOAST compression method (pglz/lz4) end-to-end: STUB (only `toast_compression::INVALID_COMPRESSION_METHOD` constant referenced in src/backend/access/common/tupdesc.rs, no compress/decompress or `pg_column_compression` fn); src/backend/commands/tablecmds.rs: STUB (`not_yet_reachable("DefineRelation: inheritance")` / `"partitioning"`, and no COMPRESSION/STORAGE column option handling); src/backend/commands/view.rs: STUB (`not_yet_reachable` for materialized-view-adjacent CREATE OR REPLACE VIEW paths; no CREATE MATERIALIZED VIEW handler found) | self-contained |
| compression_pglz | OUT_OF_SCOPE | CREATE FUNCTION ... LANGUAGE C (loads regress.so `test_pglz_compress`/`test_pglz_decompress`), bytea `decode()`/comparison, direct PGLZ corrupt-data edge cases | LANGUAGE C dynamic function loading/execution: MISSING (src/backend/commands/functioncmds.rs has no C-language fmgr shared-library loader, only catalog registration); `decode`/`encode` bytea functions: MISSING (grep for "decode"/"encode"/"repeat" in pg_proc.rs and adt/varlena.rs finds no registration, only `byteain` literal parsing) | self-contained |
| constraints | OUT_OF_SCOPE | DEFAULT/CHECK/PRIMARY KEY/UNIQUE/EXCLUDE/NOT NULL constraints, NOT ENFORCED/ENFORCED, DEFERRABLE/INITIALLY DEFERRED + SET CONSTRAINTS, ON CONFLICT DO NOTHING/UPDATE, EXCLUDE USING gist on CIRCLE type, INHERITS + NO INHERIT constraints, PARTITION BY LIST/RANGE/HASH + ATTACH/DETACH PARTITION, NOT NULL NO INHERIT, CREATE ROLE + SET SESSION AUTHORIZATION, COMMENT ON CONSTRAINT | SET CONSTRAINTS / deferred constraint enforcement: MISSING (no `SetConstraintsStmt` handling found anywhere in src/backend); INHERITS (CREATE TABLE ... INHERITS, ALTER TABLE ... INHERIT): STUB (`not_yet_reachable("DefineRelation: inheritance")` in src/backend/commands/tablecmds.rs); PARTITION BY / ATTACH PARTITION: STUB (`not_yet_reachable("DefineRelation: partitioning")` in tablecmds.rs); EXCLUDE constraint + GIST index AM: MISSING (no exclusion-constraint code in catalog/index.rs or tablecmds.rs; access/gist is absent -- whole AM missing); CREATE ROLE: MISSING (no command handler under src/backend/commands, only the parsenode struct exists in the header skeleton) | self-contained |
| conversion | OUT_OF_SCOPE | CREATE/DROP CONVERSION (already supported), CREATE FUNCTION ... LANGUAGE C (loads regress.so `test_enc_setup`/`test_enc_conversion`), PL/pgSQL function body (`test_conv` wrapper with BEGIN/EXCEPTION), CREATE USER + SET/RESET SESSION AUTHORIZATION, built-in encoding conversion functions, bytea literals | LANGUAGE C dynamic function loading/execution: MISSING (src/backend/commands/functioncmds.rs has no shared-library loader); PL/pgSQL body execution: MISSING (no plpgsql module found anywhere in src/); CREATE USER/ROLE: MISSING (no command handler, only struct stub in src/nodes/parsenodes.rs) | self-contained |
| copy | OUT_OF_SCOPE | COPY TO/FROM text and CSV (quote/escape/header/header match/on_error ignore), COPY (query) TO, COPY FROM stdin literal `\.` handling, PARTITION BY LIST + ATTACH PARTITION + multi-insert COPY into partitions, BEFORE INSERT trigger on a partition, pg_stat_progress_copy view + PL/pgSQL trigger reading it, CREATE FOREIGN DATA WRAPPER/SERVER/FOREIGN TABLE, CREATE MATERIALIZED VIEW + REFRESH, point type (`location point`) | PARTITION BY / ATTACH PARTITION: STUB (`not_yet_reachable("DefineRelation: partitioning")` in src/backend/commands/tablecmds.rs) blocks all `parted_copytest*`/`parted_si*` sections; PL/pgSQL body execution: MISSING (needed for `part_ins_func`, `notice_after_tab_progress_reporting`); foreign data wrapper/server/foreign table: MISSING (no `ForeignDataWrapper`/`CreateForeignServer` handler found in src/backend/commands); materialized views: STUB (no CREATE MATERIALIZED VIEW handler in src/backend/commands/view.rs, only base CREATE VIEW); geometry `point` type: STUB/MISSING (utils/adt geometry functions largely untranslated per project status) | `emp.data` external data file (self-contained tables, no shared test_setup fixture tables) |

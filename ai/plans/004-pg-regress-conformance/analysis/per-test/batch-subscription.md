| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| subscription | OUT_OF_SCOPE | CREATE/ALTER/DROP SUBSCRIPTION, CREATE ROLE (+ pg_create_subscription predefined role), \dRs psql meta-command, pg_stat_subscription_stats, obj_description on pg_subscription, GRANT CREATE ON DATABASE, SET SESSION AUTHORIZATION | logical replication subsystem (MISSING: no CREATE SUBSCRIPTION grammar in src/backend/parser/gram.lalrpop, no executor in src/backend/commands; only catalog headers src/catalog/pg_subscription.rs, pg_publication*.rs exist); CREATE ROLE (MISSING: no CreateRoleStmt handler under src/backend/commands, needed for regress_subscription_user*) | self-contained |
| subselect | NEEDS_MODULES | uncorrelated/correlated subqueries, IN/NOT IN/ROW-compare, LATERAL, WITH RECURSIVE, hashed subplans, EXISTS, VALUES-as-subquery incl. ORDER BY/LIMIT/OFFSET inside VALUES, json_array() call inside a join subquery, generate_series, array subscripting on subquery result | src/backend/utils/adt/jsonfuncs.rs (MISSING -- no JSON module at all under src/backend/utils/adt; needed only for the one-off `json_array(1, a)` call around line 994-996) | onek, int4_tbl, int8_tbl, tenk1, road (all core LATERAL/CTE/subquery machinery itself is translated: nodeRecursiveunion.rs, nodeWorktablescan.rs, parse_clause.rs LATERAL handling) |
| sysviews | OUT_OF_SCOPE | SRF-backed system views: pg_available_extension(_versions), pg_backend_memory_contexts, pg_config, pg_cursors, pg_file_settings, pg_hba_file_rules, pg_ident_file_mappings, pg_locks, pg_prepared_statements/xacts, pg_stat_slru, pg_stat_wal(_receiver), pg_settings, pg_wait_events, pg_timezone_names/abbrevs, bump-allocator memory context via cursor+tuplesort | none of these system views/SRFs exist (MISSING: grep confirms no `pg_backend_memory_contexts`, `pg_stat_slru`, `pg_wait_events`, `pg_timezone_names/abbrevs`, `pg_available_extensions`, `pg_hba_file_rules`, `pg_ident_file_mappings`, `pg_file_settings`, `pg_config` anywhere under src/backend); this is the introspection/stats-views subsystem wholesale | self-contained |
| tablesample | OUT_OF_SCOPE | TABLESAMPLE SYSTEM/BERNOULLI clause with REPEATABLE, tablesample on view/CTE/subquery/inherited table/partitioned table, fillfactor reloption | TABLESAMPLE clause (MISSING: no `TABLESAMPLE` keyword in src/backend/parser/gram.lalrpop grammar; no SampleScan executor node; whole tablesample AM subsystem absent) | test_tablesample (self-contained) + person (inheritance) + tenk1 |
| tablespace | NEEDS_MODULES | CREATE/ALTER/DROP TABLESPACE incl. WITH reloptions and spcoptions, REINDEX (TABLESPACE ...), ALTER TABLE/INDEX/MATERIALIZED VIEW SET TABLESPACE, ALTER ... ALL IN TABLESPACE, partitioned tables+indexes with per-partition tablespace, default_tablespace GUC interplay, GRANT ON TABLESPACE, materialized views | src/backend/commands/tablespace.rs (STUB -- only create_tablespace/drop_tablespace/tablespace_oid_by_name exist; no ALTER TABLESPACE (SET/RESET options, RENAME, OWNER TO), no WITH-options/spcoptions storage, no per-relation tablespace routing); src/backend/commands/indexcmds.rs (MISSING REINDEX statement handling, needed for REINDEX (TABLESPACE ...)); matview subsystem (MISSING: no matview .rs file, needed for CREATE/ALTER/REFRESH MATERIALIZED VIEW); partitioning (MISSING: no PartitionBoundSpec/partition-by executor, needed for partitioned-table tablespace tests) | self-contained (uses testschema + regress_tblspace) |
| temp | NEEDS_MODULES | CREATE TEMP TABLE/INDEX with search-path masking, ON COMMIT DELETE ROWS/DROP (incl. CTAS and partitioned/inheritance variants), pg_temp schema function resolution, FK constraints on temp tables, PREPARE TRANSACTION rejection for temp objects, temp_buffers limit + buffer-pin exhaustion via plpgsql cursors, CHECK constraint w/ toasted pg_constraint | PL/pgSQL body execution (MISSING/STUB: `test_temp_pin` and the DO block both require LANGUAGE plpgsql function *bodies* to execute, which memory confirms is NOT_TRANSLATED -- triggers/functions fire but user-PL bodies are not executed); partitioned/inheritance ON COMMIT interaction (MISSING: no partition-by executor, per test_setup/tablespace analysis) | self-contained (temp tables), plus person/emp/student inheritance-style trees created ad hoc |
| test_setup | SETUP_BLOCKED | fixture creation: CHAR/FLOAT8/INT2/INT4/INT8/TEXT/VARCHAR _TBL, POINT_TBL (point type), onek/onek2/tenk1/tenk2 via COPY FROM file, person/emp/student/stud_emp via INHERITS, road/ihighway/shighway (path type + INHERITS), CREATE TYPE ... AS ENUM, CREATE TYPE ... AS RANGE, CREATE OPERATOR CLASS ... USING hash, LANGUAGE C functions (binary_coercible), SQL-body functions using sha256/encode | src/backend/utils/adt geometry module (MISSING: no point/path type support -- grep finds no `point_in`/`path_in`/geo module -- blocks POINT_TBL, person.location, road.thepath); access/hash index AM (MISSING: no hash AM, blocks `CREATE OPERATOR CLASS ... USING hash`); range types (MISSING: no CreateRangeStmt/rangetype support beyond scattered `range` identifiers in int.rs/timestamp.rs which are unrelated numeric range checks, blocks `float8range`/`textrange`); LANGUAGE C dynamic-load functions (MISSING: no C-language function loading in functioncmds.rs, blocks `binary_coercible`); sha256/encode (MISSING: no crypto/encode builtins found under utils/adt, blocks `fipshash`) -- INHERITS itself IS handled (tablecmds.rs checks `inhRelations`) | n/a (this test builds fixtures for all others) |
| text | NEEDS_MODULES | text equality/comparison, implicit text cast in concatenation, concat/concat_ws/format/quote_literal/reverse/left/right/generate_series-driven loop, variadic array arguments | src/backend/utils/adt/varlena.rs (STUB -- only textin/out/recv/send, textcat, text_substr, texteq/ne, bttextcmp, text_starts_with exist; `concat`, `concat_ws`, `format`, `quote_literal`, `reverse`, `left`, `right` are all absent, confirmed via grep for their SQL names across src/backend and src/catalog/pg_proc.rs) | TEXT_TBL (from test_setup) |

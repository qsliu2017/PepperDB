| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| oid | NEEDS_MODULES | `oid` type in/out (incl. whitespace/tab trimming, unsigned wraparound on `-N`, overflow/invalid-syntax errors), `oid` comparison operators, `oidvector` type, `pg_input_is_valid`/`pg_input_error_info` for `oid` and `oidvector` | `pg_input_is_valid`/`pg_input_error_info` -- MISSING: no such functions exist anywhere in `src/` (not in `src/fmgr.rs`, `src/backend/utils/fmgr/fmgr.rs`, or any catalog file). This is the only blocker -- `src/backend/utils/adt/oid.rs` already has real, tested implementations of `oidin`/`oidout`/the comparison operators/`oidvectorin`/`oidvectorout` covering the rest of the test's surface (its `unimplemented!()`s are only on `oidrecv`/`oidsend`/`oidvectorrecv`/`oidvectorsend`/`oidparse`, none of which this test exercises). | self-contained (creates OID_TBL) |

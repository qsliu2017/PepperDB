| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| predicate | NEEDS_MODULES | EXPLAIN (COSTS OFF) on IS NULL/IS NOT NULL qual reduction (const-FALSE folding, redundant-qual removal) over restriction and outer-join clauses, OR-clause folding, table inheritance (`INHERITS`) with NOT NULL constraint propagation to child scans | src/backend/commands/explain.rs (MISSING: no explain.rs under src/backend/commands -- `find src/backend -iname "*explain*"` returns nothing; `grep -rn ExplainStmt src/backend` = no hits, so `EXPLAIN (COSTS OFF) SELECT ...` cannot run at all), src/backend/optimizer (MISSING: no predtest.c-equivalent -- `grep -rn predtest src/backend/optimizer` = no hits; the NullTest-implied-by-NOT-NULL-constraint reduction and OR-clause-all-branches-false reduction that this test exists to exercise are unimplemented), src/backend/optimizer/plan/planner.rs (STUB: table inheritance explicitly disabled -- comment at planner.rs:595-606 says "no table has subclasses -> clear every RELATION's `inh`", so `CREATE TABLE ... () INHERITS (...)` scans cannot produce the per-child qual pruning the test checks) | self-contained (creates own pred_tab/pred_parent/pred_child tables) |
| prepare | NEEDS_MODULES | PREPARE/EXECUTE/DEALLOCATE [PREPARE]/DEALLOCATE ALL, pg_prepared_statements system view, parameterized queries with explicit types incl. `unknown`, CREATE TABLE AS EXECUTE ... WITH [NO] DATA, error paths (redefine without DEALLOCATE, wrong param count/types, invalid type) | src/backend/commands/prepare.rs (present, largely implemented: prepare_query/execute_query/deallocate_query/pg_prepared_statements table exist) -- but relies on fixture tables tenk1/road from test_setup.sql (see fixtures) and on `CREATE TABLE ... AS EXECUTE` DML path; spot risk only, no missing subsystem found via grep (`prepared_statement_exists`, `store_prepared_statement`, `drop_all_prepared_statements` all present in prepare.rs) | test_setup.sql tables tenk1 (widely-typed 10k-row table) and road (used for `q7(unknown)` prepare) -- NOT self-contained |
| prepared_xacts | OUT_OF_SCOPE | PREPARE TRANSACTION 'gid' (two-phase commit), COMMIT PREPARED/ROLLBACK PREPARED, pg_prepared_xacts view, duplicate-gid detection, SSI write-skew abort under PREPARE TRANSACTION, session/xact-level advisory-lock conflict detection at prepare time, subtransactions + shared invalidation across a prepared xact, row-level lock visibility (FOR SHARE/FOR UPDATE NOWAIT) held by a prepared xact, multixact creation, `\c -` reconnect | src/backend/access/transam/xact.rs (STUB: `PrepareTransactionBlock` explicitly documented as a stub -- "PrepareTransaction (two-phase) is a stub. TODO(twophase)" at line 1370, and `prepare_transaction_stub()` at line 1462-1465 calls `unimplemented!("two-phase PrepareTransaction is deferred (twophase.c)")`; matches repo-wide NOT TRANSLATED note "2PC/PREPARE TRANSACTION"), no pg_prepared_xacts view found (`grep -rln pg_prepared_xacts src/backend` = no hits) | self-contained (own pxtest1..4, pxtest_rowlock tables) but requires `max_prepared_transactions >= 2` GUC gate (`\gset`/`\if`/`\quit` psql meta-commands at top, which do exist in src/fe_utils/conditional.rs) |
| privileges | NEEDS_MODULES | CREATE/DROP ROLE/USER, GRANT/REVOKE role membership incl. WITH ADMIN OPTION/GRANTED BY/CASCADE, circular admin-option detection, SET ROLE/SET SESSION AUTHORIZATION/RESET ROLE, pg_auth_members/pg_authid catalogs, predefined roles (pg_read_all_data etc.), DROP OWNED/REASSIGN OWNED, table/column/schema/function/language/large-object GRANT+REVOKE with privilege enforcement, default privileges, row-level security-adjacent grant checks, large objects (lo_unlink) | src/backend/commands (MISSING: no user.rs/role.rs -- `find src/backend/commands -iname "*user*" -o -iname "*role*"` returns nothing; `grep -n CreateRoleStmt src/backend/parser/gram.lalrpop` = no hits, so `CREATE USER`/`CREATE ROLE` do not parse), src/backend/catalog/aclchk.rs (STUB: `execute_grant_stmt` only reaches `GrantTargetType`/`ObjectType` = TABLE -- other target/object types hit `unimplemented!("ExecuteGrantStmt: ... not yet reachable")` at lines 67/70; named roles besides seeded ones hit `unimplemented!("... named role ... not yet seeded in pg_authid")` at line 305), pg_authid/pg_auth_members catalog tables (MISSING as real tables: only referenced by bootstrap/miscinit for the fixed bootstrap superuser, no ALTER/GRANT-role machinery), large objects (MISSING: `lo_unlink`/pg_largeobject not found in src/backend) | self-contained (creates/drops own regress_priv_* roles and tables), matches repo-wide NOT TRANSLATED notes "privilege/RLS ENFORCEMENT; roles(pg_authid)" |
| psql | OUT_OF_SCOPE | psql-only meta-commands and client behavior: \set/\echo special variables (ON_ERROR_ROLLBACK, AUTOCOMMIT, FETCH_COUNT), \g/\gx with pset options, extended-protocol meta-commands \parse/\bind_named/\close_prepared, \gset/\gdesc/\gexec, \setenv/\getenv, \pset formatting, \d meta-commands, and hundreds more psql-client-specific behaviors | psql client binary (MISSING: PepperDB is a server-only 1:1 port -- `find . -iname "*bin*"` under src/ shows no `bin/psql` program; only `src/fe_utils/psqlscan.rs` and `psqlscan_int.rs` header-stub files exist, which are lexer scaffolding, not a working psql REPL with \-command dispatch, variable substitution, or pset). This entire test is a client-side psql feature test, not a server/SQL-engine test | self-contained (declares own variables/queries inline) |
| psql_crosstab | OUT_OF_SCOPE | psql \crosstabview meta-command (pivots query results into a cross-tab display), column-selection by name/position/quoting, error paths for column count/ambiguity | psql client binary (MISSING: \crosstabview is implemented entirely in the psql client (`src/bin/psql/crosstabview.c` in PG); `grep -rln crosstabview src` under PepperDB returns nothing at all -- no client program exists to host this feature) | self-contained (creates own ctv_data table) |
| psql_pipeline | OUT_OF_SCOPE | psql pipelining meta-commands \startpipeline/\endpipeline/\syncpipeline/\flush/\bind/\sendpipeline/\parse/\bind_named, PIPELINE_COMMAND_COUNT/PIPELINE_SYNC_COUNT/PIPELINE_RESULT_COUNT variables, libpq pipeline mode | psql client binary + libpq pipeline mode (MISSING: pipelining is a libpq/psql client feature -- no psql program exists in this server-only port; `grep -rln startpipeline src` = no hits). The server-side extended-query-protocol pieces (Parse/Bind/Execute/Sync) partially exist per repo notes ("PREPARE/EXECUTE+extended proto" is TRANSLATED), but the test itself drives them exclusively through psql meta-commands that don't exist here | self-contained (creates own psql_pipeline table) |
| publication | OUT_OF_SCOPE | CREATE/ALTER/DROP PUBLICATION, publication options (publish=insert/update/delete/truncate, publish_via_partition_root, publish_generated_columns), FOR ALL TABLES / FOR TABLE / FOR TABLES IN SCHEMA, \dRp/\dRp+ psql meta-commands, pg_publication/pg_publication_rel/pg_publication_namespace catalogs, table partitioning (`PARTITION BY LIST`), row filters (WHERE) and column lists on publications, CREATE ROLE ... LOGIN SUPERUSER | logical-replication publication subsystem (MISSING entirely: `grep -rln CreatePublication\|pg_publication src/backend` = zero hits anywhere in src/backend), catalog/pg_publication* (MISSING catalogs), commands/publicationcmds.rs equivalent (MISSING file), table partitioning (MISSING: `PARTITION BY`/`PARTITION OF` not translated per repo-wide notes), roles/CREATE ROLE ... LOGIN SUPERUSER (MISSING: no CreateRoleStmt in grammar, see privileges analysis). Matches repo-wide NOT TRANSLATED note "publication/subscription/replication" | self-contained (creates own roles/schema/tables), but also needs psql \dRp/\dRp+ meta-commands (client-side, also MISSING) |

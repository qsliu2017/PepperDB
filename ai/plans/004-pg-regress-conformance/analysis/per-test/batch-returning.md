| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| returning | NEEDS_MODULES | INSERT/UPDATE/DELETE RETURNING incl. subplans/initplans, joins (FROM/USING), rules+views RETURNING, whole-row Var, SRF returning setof composite, aliased target, RETURNING OLD/NEW (PG18: `RETURNING old.*`, `new.*`, `RETURNING WITH (OLD AS o, NEW AS n) ...`), ON CONFLICT DO UPDATE RETURNING OLD/NEW, cross-partition UPDATE RETURNING, `\sf` deparse | (1) src/backend/parser/gram.lalrpop `ReturningClause` STUB - only `RETURNING target_list` implemented, no `returning_with_clause`/old./new. alias support (comment: "OLD/NEW alias form grows later"); blocks the entire second half of the test (RETURNING OLD/NEW section) (2) `CREATE TABLE ... INHERITS` MISSING in gram.lalrpop - blocks the inheritance-cases section (foochild INHERITS foo) (3) PARTITION BY/partitioned-table RETURNING section also needs partitioning (declared NOT TRANSLATED) | int8_tbl, int4_tbl (from test_setup); rest self-contained (temp tables foo/foochild/voo/etc.) |
| roleattributes | NEEDS_MODULES | CREATE ROLE/USER with SUPERUSER/INHERIT/CREATEROLE/CREATEDB/LOGIN/REPLICATION/BYPASSRLS attrs, ALTER ROLE, DROP ROLE/USER, SELECT from pg_authid | src/commands/user.rs (= src/backend/commands/user.rs) STUB - `CreateRole`, `AlterRole` both `unimplemented!()`; grammar also lacks `CreateRoleStmt`/`AlterRoleStmt` (no ROLE/USER keyword handling found in gram.lalrpop) - why: entire test is CREATE/ALTER/DROP ROLE round-trips, none of which can execute | self-contained (only pg_authid catalog reads, no test_setup tables) |
| rowsecurity | OUT_OF_SCOPE | Full RLS: CREATE POLICY (PERMISSIVE/RESTRICTIVE), ALTER/DROP POLICY, ENABLE/FORCE ROW LEVEL SECURITY, SELECT/INSERT/UPDATE/DELETE/MERGE under RLS, ON CONFLICT + RLS, RLS w/ views (security_barrier, security_invoker), RLS w/ inheritance+partitioning, RLS recursion checks, TABLESAMPLE, WHERE CURRENT OF, COPY+RLS, pg_policies/pg_stats interaction, roles/GRANT membership | (1) src/rewrite/rowsecurity.rs (`get_row_security_policies`) STUB - core RLS qual injection unimplemented (2) src/utils/rls.rs (`check_enable_rls`) STUB (3) src/commands/policy.rs (`CreatePolicy`/`AlterPolicy`/etc.) all STUB (4) src/commands/user.rs `CreateRole` STUB - test needs many roles (5) grammar lacks CreatePolicyStmt and `GRANT role TO role` / `GRANT ... ON SCHEMA` forms (6) TABLESAMPLE, partitioning also needed | self-contained (creates its own schema/tables); needs `fipshash()` function which is entirely MISSING (used pervasively for test data generation) |
| rowtypes | NEEDS_MODULES | composite type I/O (ROW() construct/parse, nested composite, whole-row Var, field access `(x).f`, dropped-column handling), row comparison operators (`<,<=,=,<>,>=,>` and `record_image_*` `*<` family), ROW IN/= ANY, indexable row comparisons, `pg_input_is_valid`/`pg_input_error_info`, arrays of anonymous rowtypes, `row_to_json`, composite cast to text, TOAST of composite values, `point` type (cantcompare test), INHERITS (tt2/tt3) | (1) no `rowtypes.rs` file anywhere under src/ - MISSING: record I/O (record_in/record_out), record_eq/record_cmp/record_image_cmp comparison operators entirely absent (2) `pg_input_is_valid`/`pg_input_error_info` MISSING (grep found nothing) (3) `row_to_json` MISSING (json support declared NOT TRANSLATED) (4) point/geometry type MISSING (declared NOT TRANSLATED) (5) `CREATE TABLE ... INHERITS` MISSING in grammar - blocks tt2/tt3 no-op-conversion tests | tenk1, int8_tbl |
| rules | NEEDS_MODULES | CREATE/ALTER/DROP RULE (ON INSERT/UPDATE/DELETE/SELECT, DO INSTEAD/ALSO, qualified, multi-statement, DO NOTHING, `_RETURN`), views w/ rules acting like updatable tables, rule firing order, INHERITS-based constraint-update cascades, MERGE + rules interaction, ON CONFLICT + rule, `pg_get_viewdef`/`pg_get_ruledef`/`pg_get_functiondef`/`\d+` deparse, ALTER TABLE ENABLE/DISABLE RULE, session_replication_role, security_invoker views + rules | (1) `pg_get_viewdef`, `pg_get_ruledef`, `pg_get_functiondef` MISSING from src/backend/utils/adt/ruleutils.rs (only `pg_get_constraintdef` stub + basic helpers exist) - blocks large \"check ruleutils working\" + `\d+` sections (2) `CREATE TABLE ... INHERITS` MISSING in grammar - blocks constraint-update/delete cascade tests and `id`/`test_1..3` inheritance-update-planning tests (3) rule creation/ON SELECT rule itself: need to verify src/commands/rulescmds equivalent - view+rule DML rewriting is core to the whole file and interacts with rowsecurity/policy infra already found stubbed elsewhere in this project, but basic CREATE RULE/view rewriting for INSERT/UPDATE/DELETE appears to be the primary untranslated surface given ruleutils gaps make deparse-checks fail outright | self-contained (all rtest_*/rule_* local tables); uses int4_tbl in later CTE dedup section |
| sanity_check | READY | VACUUM (no args), catalog sanity SELECTs joining pg_class/pg_namespace/pg_attribute/pg_index checking every system table has a unique OID index, and relfilenode-zero check for views/composites/foreign tables/partitioned tables/indexes | none - pg_class, pg_namespace, pg_attribute, pg_index catalogs all exist and populated; VACUUM (no-arg form) is translated per M13 milestone | self-contained (only queries system catalogs) |
| security_label | OUT_OF_SCOPE | `SECURITY LABEL [FOR provider] ON <object> IS 'label'` (all error-path only, no plugin loaded), CREATE USER/TABLE/VIEW/FUNCTION/DOMAIN, ALTER ... OWNER TO | (1) `SECURITY LABEL` statement entirely MISSING from src/backend/parser/gram.lalrpop (no LABEL keyword/SecLabelStmt production at all) (2) src/commands/seclabel.rs STUB - `ExecSecLabelStmt`, `SetSecurityLabel`, `GetSecurityLabel` all `unimplemented!()` (3) CREATE ROLE (`CREATE USER`) STUB per roleattributes finding above | self-contained |
| select | NEEDS_MODULES | btree index scans + ORDER BY (asc/desc/NULLS FIRST/LAST, USING operator), partial index planning (WHERE clause on CREATE INDEX), inheritance query (`person*` old-style + INHERITS fixture tables emp/student), whole-row Var from subquery, VALUES lists (incl. standalone/UNION member/no-column), LATERAL, corner cases (SRF ORDER BY, X=X strictness), empty-partition partitioned table planning | (1) `CREATE INDEX ... WHERE <predicate>` (partial index) MISSING in grammar (`IndexStmt` comment explicitly says WHERE/partial "grows at their milestones") - blocks the whole \"partial btree indexes\" section (2) `CREATE TABLE ... INHERITS` MISSING in grammar - test_setup.sql itself creates `emp`/`student` INHERITS(person), so fixture setup fails, and `person*` legacy-inheritance-scan syntax needs inheritance support too (3) `PARTITION BY LIST` w/ no partitions - partitioning declared NOT TRANSLATED, blocks final "no partitions" test | onek, onek2, person/emp/student (INHERITS-based, blocked), int8_tbl |

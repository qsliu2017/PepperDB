| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| vacuum_parallel | NEEDS_MODULES | `SET`/`RESET` parallel-vacuum GUCs, `CREATE TABLE ... WITH (autovacuum_enabled=off)`, multi `CREATE INDEX` (btree, incl. dedup), `VACUUM (PARALLEL n, INDEX_CLEANUP ON)`, `pg_relation_size()`, `pg_size_bytes()`, `current_setting()`, `pg_class` introspection | src/backend/utils/misc/guc_tables.rs (STUB: `max_parallel_maintenance_workers`/`min_parallel_index_scan_size` not registered -> bare `SET` errors "unrecognized configuration parameter"); src/backend/utils/adt/dbsize.rs (MISSING: no file exists, no `pg_relation_size`/`pg_size_bytes` fmgr fn anywhere in src/backend); src/backend/commands/vacuum.rs (STUB: `parse_vacuum_options` only recognizes full/freeze/verbose/analyze DefElems -- `parallel`/`index_cleanup` are silently dropped and `nworkers` is hardcoded to 0, so no parallel-vacuum execution/cost-model path exists) | self-contained |
| varchar | NEEDS_MODULES | `CREATE TEMP TABLE`, `varchar(n)` typmod truncation/length-check semantics, comparison ops, `pg_input_is_valid`/`pg_input_error_info` soft-error API | src/backend/utils/adt/varlena.rs (STUB: only text/bytea/unknown I/O translated -- no `varcharin`/typmod-aware length enforcement; "value too long for type" not found anywhere in src/backend, so `INSERT` of over-length values won't raise the expected error); src/backend/utils/fmgr (MISSING: `pg_input_is_valid`/`pg_input_error_info` soft-error-call API not implemented anywhere) | test_setup VARCHAR_TBL (re-created locally first, then the shared fixture) |
| window | NEEDS_MODULES | window functions (rank/dense_rank/percent_rank/cume_dist/ntile/lag/lead/first_value/last_value/nth_value/row_number), `PARTITION BY`/`ORDER BY`/named `WINDOW`, frame clauses ROWS/RANGE/GROUPS with offsets and EXCLUDE, mixed agg+window, subplans in frame offsets | src/backend/executor/nodeWindowAgg.rs (STUB: GROUPS frame mode, frame EXCLUDE clauses, and non-Const frame offsets are explicit `unimplemented!()`; window.sql exercises GROUPS extensively (lines ~920-1200) and RANGE BETWEEN with offsets, hitting these paths) | tenk1, empsalary (self-contained temp table), int4_tbl |
| with | NEEDS_MODULES | `WITH`/`WITH RECURSIVE` CTEs, `UNION`/`UNION ALL` in recursive terms, `CREATE RECURSIVE VIEW`/`CREATE OR REPLACE RECURSIVE VIEW`, CTE `SEARCH DEPTH/BREADTH FIRST` and `CYCLE` clauses, deep CTE nesting, data-modifying CTEs later in file | src/backend/parser/gram.lalrpop (STUB: `common_table_expr` grammar has no `opt_search_clause`/`opt_cycle_clause` production -- SEARCH/CYCLE syntax will fail to parse); src/backend/commands/view.rs (MISSING: no `CREATE RECURSIVE VIEW` handling -- `define_view`/grammar has plain `ViewStmt` only, no RECURSIVE keyword path, and no `nums`-style recursive-view rewrite to a WITH RECURSIVE VIEW) | self-contained (uses WITH/temp CTEs plus one small tree example later); some data-modifying-CTE cases may hit DML paths |
| without_overlaps | OUT_OF_SCOPE | `PRIMARY KEY (... WITHOUT OVERLAPS)`, `UNIQUE (... WITHOUT OVERLAPS)`, `FOREIGN KEY (..., PERIOD col) REFERENCES ... (..., PERIOD col)`, range/multirange PK columns (`int4range`, `daterange`, `datemultirange`), custom range types, `\d` introspection of temporal constraints, GiST-backed exclusion constraint under the hood | access/gist (MISSING: WITHOUT OVERLAPS temporal keys are implemented via an implicit exclusion constraint, which requires the GiST AM -- absent entirely, only nbtree exists); src/backend/parser/gram.lalrpop (MISSING: no `WITHOUT OVERLAPS`/`PERIOD` grammar productions at all in ColumnDef/TableConstraint/FK); src/backend/utils/adt/rangetypes.rs + multirangetypes.rs (MISSING: only header stubs at src/utils/rangetypes.rs and src/utils/multirangetypes.rs exist, no backend/utils/adt implementation) | self-contained (creates its own temporal_rng/temporal_mltrng tables) |
| write_parallel | OUT_OF_SCOPE | parallel-plan GUCs (`parallel_setup_cost`, `parallel_tuple_cost`, `min_parallel_table_scan_size`, `max_parallel_workers_per_gather`), `CREATE TABLE AS`/`SELECT INTO` under parallel plan, `CREATE MATERIALIZED VIEW`, `CREATE UNIQUE INDEX` on matview, `REFRESH MATERIALIZED VIEW [CONCURRENTLY]`, `PREPARE`/`EXECUTE` combined with CTAS, `EXPLAIN (COSTS OFF)` | src/backend/commands/matview.rs (MISSING: no file/grammar for `CREATE MATERIALIZED VIEW`/`REFRESH MATERIALIZED VIEW` anywhere in gram.lalrpop or commands/); src/backend/utils/misc/guc_tables.rs (STUB: the four parallel-plan GUCs above are not registered, so the leading `SET` statements fail immediately with "unrecognized configuration parameter") | tenk1 |
| xid | NEEDS_MODULES | `xid`/`xid8` I/O + comparisons + `xid8cmp`, `min`/`max` aggregates over xid8, hash+btree index on xid8, `pg_snapshot` type I/O + `pg_snapshot_xmin/xmax/xip`, `pg_visible_in_snapshot`, `pg_current_xact_id[_if_assigned]`, `pg_xact_status`, `pg_input_is_valid`/`pg_input_error_info`, PL/pgSQL exception block | src/backend/utils/adt (MISSING: no xid.rs/xid8.rs/txid.rs equivalent -- `xidin`/`xidout`/`xid8in`/`xid8out`/`xid8cmp`/`pg_snapshot_in` etc. not found anywhere in src/backend; only OID constants exist via genbki, no fmgr bodies); src/backend/utils/adt (MISSING: `pg_current_xact_id`, `pg_current_xact_id_if_assigned`, `pg_xact_status`, `pg_visible_in_snapshot`, `pg_current_snapshot` not implemented); PL/pgSQL body execution (OUT_OF_SCOPE dependency: the final `test_future_xid_status` block needs PL/pgSQL EXCEPTION handling, separately unimplemented) | self-contained |
| xml | OUT_OF_SCOPE | `xml` type I/O + validation, `xmlcomment`/`xmlconcat`/`xmlelement`/`xmlattributes`/`xmlforest`/`xmlparse`/`xmlpi`/`xmlroot`/`xmlserialize` (INDENT/NO INDENT), `xmlbinary` GUC, casts to/from xml, `pg_input_is_valid`/`pg_input_error_info` | src/backend/utils/adt/xml.rs (MISSING: no file exists under src/backend; only a header stub at src/utils/xml.rs with no bodies -- the entire libxml2-backed XML type/function family is untranslated) | emp (test_setup fixture, via `xmlforest(name, age, salary) FROM emp`) |

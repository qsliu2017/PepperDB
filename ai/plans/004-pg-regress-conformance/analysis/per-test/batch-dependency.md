| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| dependency | OUT_OF_SCOPE | CREATE/DROP USER/GROUP/ROLE, GRANT/REVOKE with GROUP grantee + TRIGGER/MAINTAIN privs, DROP OWNED BY, REASSIGN OWNED BY, SET SESSION AUTHORIZATION, ALTER TABLE OWNER TO, ALTER DEFAULT PRIVILEGES, serial/sequence OWNED BY, plpgsql function, enum/range type dependency, `\z`/`\d`/`\dt` psql meta-commands | roles subsystem (MISSING: no `CreateRoleStmt`/`DropRoleStmt` in `src/backend/parser/gram.lalrpop`, no role catalog commands under `src/backend/commands`) -- needed for CREATE USER/GROUP/ROLE; `src/backend/commands/dropcmds.rs` + `src/backend/catalog/objectaddress.rs` (STUB: DROP OWNED / REASSIGN OWNED not in grammar at all, and `get_object_address` only resolves TABLE/INDEX/VIEW/MATVIEW/SEQUENCE/FOREIGN_TABLE/TYPE/DOMAIN/SCHEMA/FUNCTION/PROCEDURE/COLLATION/CONVERSION -- other kinds `not_yet_reachable`); PL/pgSQL body execution (MISSING, no plpgsql interpreter) -- needed for `CREATE FUNCTION deptest_func() ... LANGUAGE plpgsql` | self-contained |
| domain | NEEDS_MODULES | CREATE/ALTER/DROP DOMAIN with DEFAULT/NOT NULL/CHECK constraints (incl. ALTER DOMAIN ADD/DROP/VALIDATE CONSTRAINT, ADD NOT NULL, NOT VALID), domains over arrays/composites/domains-of-domains, pg_input_is_valid/pg_input_error_info, pg_basetype, plpgsql function params/locals of domain type, information_schema.domains/domain_constraints/column_domain_usage, COPY domain enforcement, rules on domain columns | `src/backend/commands/typecmds.rs` (STUB: `define_domain` panics via `not_yet_reachable("DefineDomain: domain constraints (DEFAULT/NOT NULL/CHECK)")` whenever `stmt.constraints` is non-empty -- this test's very first statements use CHECK/DEFAULT/NOT NULL, and there is no `ALTER DOMAIN` command at all); PL/pgSQL execution (MISSING) -- needed for domain-typed function params/locals; `information_schema` catalog views (MISSING: no `information_schema` schema/views found under `src/backend/catalog`) | self-contained |
| drop_if_exists | NEEDS_MODULES | DROP ... IF EXISTS for TABLE/VIEW/INDEX/SEQUENCE/SCHEMA/TYPE/DOMAIN/USER/ROLE/GROUP/COLLATION/CONVERSION/TEXT SEARCH PARSER-DICTIONARY-TEMPLATE-CONFIGURATION/EXTENSION/FUNCTION/AGGREGATE/OPERATOR/LANGUAGE/CAST/TRIGGER/RULE/FOREIGN DATA WRAPPER/SERVER/OPERATOR CLASS/OPERATOR FAMILY/ACCESS METHOD/DATABASE(FORCE), CREATE CONVERSION, CREATE TEXT SEARCH DICTIONARY, ambiguous-function-name error, CREATE PROCEDURE | `src/backend/catalog/objectaddress.rs` (STUB: `get_object_address` handles only TABLE/INDEX/VIEW/MATVIEW/SEQUENCE/FOREIGN_TABLE/TYPE/DOMAIN/SCHEMA/FUNCTION/PROCEDURE/COLLATION/CONVERSION; AGGREGATE/OPERATOR/LANGUAGE/CAST/TRIGGER/RULE/FDW/SERVER/OPCLASS/OPFAMILY/ACCESS METHOD/TEXT SEARCH * all `not_yet_reachable`); role commands (MISSING: no CREATE/DROP USER/ROLE/GROUP grammar); `src/backend/parser/gram.lalrpop` (MISSING productions: DROP OPERATOR/AGGREGATE/CAST/TRIGGER/RULE/FDW/SERVER/OPERATOR CLASS-FAMILY/ACCESS METHOD, DROP DATABASE ... (FORCE), CREATE PROCEDURE, CREATE TEXT SEARCH DICTIONARY) | self-contained |
| drop_operator | NEEDS_MODULES | CREATE OPERATOR (with COMMUTATOR/NEGATOR), DROP OPERATOR, pg_operator oprcom/oprnegate consistency queries | `src/backend/parser/gram.lalrpop` (MISSING: no `CreateOperatorStmt` production -- grep for `CREATE OPERATOR`/operator DefElem list found nothing); operator DDL command module (MISSING: no `src/backend/commands/operatorcmds.rs`-equivalent; only `dropcmds.rs`/`indexcmds.rs`/`define.rs` reference "operator" and none implement CREATE OPERATOR) | self-contained (reads pg_catalog.pg_operator directly) |
| encoding | OUT_OF_SCOPE | CREATE FUNCTION ... LANGUAGE C (loadable C test module `regress.so`), multibyte encoding internals (pg_mblen/pg_encoding_mblen family for EUC_JP/EUC_CN/EUC_TW/UTF8/MULE_INTERNAL), bytea<->text conversion helpers, regexp_replace, reverse, substring on toasted multibyte text, TOAST slicing of long UTF8 text, convert_to, plpgsql, U& string literal escapes, json cast of text | C extension-module loading (OUT_OF_SCOPE: `LANGUAGE C` dynamic library loading of `regress.so` test harness is not part of the SQL engine translation); PL/pgSQL execution (MISSING) -- `test_encoding` function body; multibyte encoding conversion internals beyond UTF8 (mule/euc code in `src/backend/utils/mb` not verified complete for this breadth) | self-contained |
| enum | NEEDS_MODULES | CREATE TYPE ... AS ENUM, ALTER TYPE ADD VALUE [BEFORE/AFTER] [IF NOT EXISTS], ALTER TYPE RENAME VALUE, enum_first/enum_last/enum_range, pg_input_is_valid/pg_input_error_info, enum comparison/aggregates (min/max), enum[] arrays, DOMAIN over enum, btree AND hash index on enum column, RI/FK trigger on enum, plpgsql function with anyenum, transactional ADD VALUE safety semantics | `src/backend/commands/typecmds.rs` (STUB: `define_enum` doc comment says "STAGED: the pg_enum label rows ... are not seeded yet" -- no pg_enum rows are written at all, so `enum_in`/ordering/comparison have no data); enum support functions `enum_first`/`enum_last`/`enum_range`/`ALTER TYPE ... ADD VALUE`/`RENAME VALUE` (MISSING: no enum.rs found under `src/backend/utils/adt`, no ALTER TYPE ADD VALUE in grammar); access/hash index AM (MISSING: no hash index files under `src/backend/access`); PL/pgSQL execution (MISSING) -- needed for `echo_me(anyenum)` | self-contained |
| equivclass | OUT_OF_SCOPE | CREATE TYPE base form (INPUT=/OUTPUT=/LIKE=), CREATE CAST ... WITHOUT FUNCTION, CREATE OPERATOR, ALTER OPERATOR FAMILY ... ADD OPERATOR/FUNCTION, ALTER OPERATOR ... SET (HASHES), planner equivalence-class internals via EXPLAIN, row-level security (ENABLE ROW LEVEL SECURITY, CREATE POLICY), self-join removal, mergejoin/hashjoin planning, uses tenk1 fixture | `src/backend/parser/gram.lalrpop` (MISSING: no CREATE TYPE base-type form, no CREATE OPERATOR, no CREATE CAST statement, no ALTER OPERATOR FAMILY/ALTER OPERATOR); Row-Level Security (MISSING: no RLS/CREATE POLICY subsystem); role commands (MISSING: CREATE USER) | fixtures: tenk1 (plus self-defined ec0/ec1/ec2/tbl_nocom tables) |
| errors | NEEDS_MODULES | Deliberate parser/semantic error cases: bad relation/attribute names, empty SELECT/DISTINCT list, DELETE/DROP TABLE missing name, ALTER TABLE RENAME conflicts, ABORT/END outside transaction, CREATE AGGREGATE type mismatches, DROP INDEX/AGGREGATE/FUNCTION/TYPE/OPERATOR/RULE malformed or missing-object errors, division-by-zero across int2/int4/int8/numeric/float4/float8, psql syntax-error location reporting for long/multi-line statements | CREATE/DROP AGGREGATE command (MISSING: no aggregate DDL found -- `not_yet_reachable` path in `get_object_address` for AGGREGATE, and no `CreateAggregateStmt` handling seen in `commands/`); DROP OPERATOR/RULE error paths (STUB via `get_object_address` `not_yet_reachable` for OPERATOR/RULE); exact error-message/SQLSTATE-per-error-site fidelity is broad (every PG error site maps to distinct SQLSTATE per repo notes; this repo currently collapses many to XX002) -- affects expected-output matching though the underlying division-by-zero (int/numeric/float) IS correctly implemented in `src/backend/utils/adt/{int,float,numeric}.rs` | self-contained |

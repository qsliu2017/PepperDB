| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| index_including | OUT_OF_SCOPE | CREATE INDEX/ALTER TABLE ADD UNIQUE/PK ... INCLUDE (...); box type; ALTER TABLE ADD ... USING INDEX; CREATE INDEX CONCURRENTLY; REINDEX; ALTER INDEX ALTER COLUMN SET STATISTICS; gist/spgist/brin/gin/hash AM rejection test; pg_get_indexdef/pg_get_constraintdef; name-typed index columns | src/backend/parser/gram.lalrpop (IndexStmt/TableConstraint - MISSING: no INCLUDE clause in grammar at all, "grows at milestones" comment confirms unimplemented), src/backend/utils/adt/geo_decls.rs (STUB - box type has header decls only, no adt fn file/fmgr binding for box() constructor or box I/O), access/gist+access/spgist (MISSING - AM-rejection subtest needs these AMs to exist so CREATE INDEX ... USING gist/spgist can be attempted and correctly fail only for btree-incompatible ops), src/backend/commands/tablecmds.rs (STUB - ALTER TABLE ADD CONSTRAINT ... USING INDEX / CONCURRENTLY not found) | self-contained |
| index_including_gist | OUT_OF_SCOPE | CREATE INDEX ... USING gist (...) INCLUDE (...); box/point types; EXCLUDE USING gist; CREATE INDEX CONCURRENTLY; REINDEX; index-only scan bitmap/plan toggles | access/gist (MISSING - whole GiST access method absent, only btree exists), src/backend/utils/adt/geo_decls.rs (STUB - box/point constructors and operators like <@, && not implemented, header-only) | self-contained |
| indexing | OUT_OF_SCOPE | CREATE TABLE ... PARTITION BY RANGE/LIST/HASH; partition attach/detach; partitioned indexes (ALTER INDEX ... ATTACH PARTITION); brin/spgist/gin index AMs; EXCLUDE USING gist with range types (int4range); CREATE INDEX CONCURRENTLY; REINDEX CONCURRENTLY | src/backend/parser/gram.lalrpop (MISSING - no "PARTITION BY" table clause / CreateStmt only supports plain `CREATE TABLE name (elts)`, confirmed by comment "PARTITION OF, inheritance, partition spec ... arrive at their milestones"), access/gin+access/brin+access/spgist (MISSING), src/backend/utils/adt (MISSING - int4range/range types not translated) | self-contained |
| indirect_toast | OUT_OF_SCOPE | CREATE FUNCTION ... LANGUAGE C (loadable C extension module make_tuple_indirect); TOAST indirect/external datum handling; CREATE TRIGGER ... EXECUTE PROCEDURE with PL/pgSQL body; VACUUM FREEZE | src/backend/utils/fmgr (MISSING - dynamic loading of C shared-library functions via CREATE FUNCTION ... AS 'path' LANGUAGE C not supported, PepperDB has no loadable-module fmgr path), PL/pgSQL execution (MISSING - trigger function body `update_using_indirect` is PL/pgSQL, only catalog entry supported per project rules, no body execution) | self-contained |
| inet | OUT_OF_SCOPE | inet/cidr types + full operator set (<<, <<=, >>, >>=, &&, &, \|, +, -, ~), host/abbrev/broadcast/network/masklen/netmask/hostmask/set_masklen/inet_merge/inet_same_family functions, btree+gist+spgist index AMs on inet, pg_input_is_valid/pg_input_error_info | src/utils/inet.rs (STUB - only header/struct decls translated per memory notes "inet/cidr" listed under NOT TRANSLATED; no adt fn file with inet I/O, operators, or functions), access/gist+access/spgist (MISSING - needed for inet_idx2/inet_idx3 subtests) | self-contained |
| infinite_recurse | NEEDS_MODULES | recursive SQL-language function definition + call, triggering max_stack_depth / stack-overflow detection (check_stack_depth), \set VERBOSITY sqlstate, \gset, \if/\quit meta-commands | src/miscadmin.rs (STUB - check_stack_depth(), stack_is_too_deep(), set_stack_base()/restore_stack_base() all `unimplemented!()`), SQL-language function body execution (MISSING - `select infinite_recurse()` requires executing a SQL-language function body, not just its catalog entry, per project notes "no PL/SQL body EXECUTION") | self-contained |
| inherit | OUT_OF_SCOPE | CREATE TABLE ... INHERITS (...); multiple inheritance; ALTER TABLE INHERIT/NO INHERIT; inherited UPDATE/DELETE across hierarchy; constraint/NOT NULL inheritance + coninhcount bookkeeping; table partitioning (PARTITION BY range/list, ATTACH/DETACH PARTITION); MergeAppend/Append planner tests over inheritance trees; circle type + EXCLUDE USING gist | src/backend/parser/gram.lalrpop (MISSING - CreateStmt has no INHERITS clause at all: grammar is only `CREATE TABLE name ( elts )`, no OptInherit rule), src/backend/commands/tablecmds.rs (MISSING - no inheritance-tree bookkeeping: pg_inherits population, attinhcount/coninhcount propagation, inherited ALTER TABLE fan-out), access/gist (MISSING - circle/EXCLUDE USING gist subtests), partitioning subsystem (MISSING - PARTITION BY / ATTACH-DETACH PARTITION) | tenk1, int4_tbl (used incidentally in join/subplan tests), otherwise self-contained (most tables created inline) |
| init_privs | NEEDS_MODULES | pg_init_privs catalog + count(*) query, GRANT SELECT (column-list) ON table TO CURRENT_USER (column-level privileges), GRANT on system catalogs (pg_proc, pg_authid) | src/backend/parser/gram.lalrpop (STUB - GrantStmt's Privilege rule has no column-list form i.e. no `SELECT (colname, ...)`, and GranteeList only accepts ColId, not the CURRENT_USER keyword; both needed by this test's two GRANT statements), src/catalog/pg_init_privs.rs (STUB - struct-only catalog definition, no bootstrap-seeding code found anywhere in src/backend, so `pg_init_privs` table would be empty and `count(*) > 0` would fail) | self-contained |

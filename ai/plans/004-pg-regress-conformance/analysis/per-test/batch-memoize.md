| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| memoize | OUT_OF_SCOPE | EXPLAIN (ANALYZE, COSTS OFF, ...) wrapped in a plpgsql function to normalize Memoize stats (Hits/Misses/Evictions/Memory Usage), Memoize executor node behind nested-loop joins incl. LATERAL/PlaceHolderVars, GUCs enable_hashjoin/enable_bitmapscan/enable_mergejoin/enable_seqscan/work_mem/hash_mem_multiplier/enable_partitionwise_join/parallel_* knobs, `fipshash()`, `PARTITION BY RANGE`/`PARTITION OF`, parallel Gather plans | commands/explain.rs -- MISSING (no EXPLAIN command implementation anywhere in src/backend/commands, needed for every query in this file via explain_memoize()/explain calls); executor/nodeMemoize.rs -- MISSING (no Memoize executor node; nodeModifyTable.rs/nodeMergejoin.rs exist but no nodeMemoize.rs, so the plan/executor cache-node feature is entirely absent); PL/pgSQL function body execution -- MISSING (explain_memoize is a `language plpgsql` function; only catalog-level function definitions are stored, no PL/pgSQL interpreter, per project scope notes); commands/tablecmds.rs -- STUB (`not_yet_reachable("DefineRelation: partitioning")` blocks `PARTITION BY RANGE`/`PARTITION OF` near end of file) | tenk1 |
| merge | NEEDS_MODULES | `MERGE INTO ... USING ... ON ... WHEN MATCHED/NOT MATCHED/NOT MATCHED BY SOURCE THEN UPDATE/DELETE/INSERT/DO NOTHING`, `RETURNING merge_action(), old, new, t.*`, MERGE inside CTE/COPY (should error), MERGE into view/materialized view (should error), GRANT/privilege checks for MERGE, EXPLAIN (COSTS OFF) on MERGE plans | src/backend/executor/nodeModifyTable.rs -- STUB (`ExecMerge` is `unimplemented!("ExecMerge: MERGE execution not yet reachable for this milestone")` at line ~177; MERGE statements parse into a ModifyTable plan with merge_action_lists but execution is unimplemented) | self-contained (target/source tables created in-file) |
| misc | OUT_OF_SCOPE | table inheritance (`CREATE TABLE emp (...) INHERITS (person)`, multiple inheritance `INHERITS (emp, student)`, `FROM person*`/`FROM ONLY person`), `point` geometry column type, SQL-language ("postquel") functions taking/returning composite row types with dotted field-access chains (`(p.hobbies).equipment.name`), `%TYPE` attribute references in function signatures, `CREATE FUNCTION ... LANGUAGE C` loading a shared lib (regress.so), `COPY BINARY` | commands/tablecmds.rs -- STUB (`not_yet_reachable("DefineRelation: inheritance")`: blocks CREATE TABLE ... INHERITS used by test_setup.sql's emp/student/stud_emp, which this test reads via person*/ONLY person); src/backend/utils/adt/geo.rs -- MISSING (no geometry module under src/backend; only header stub src/utils/geo_decls.rs; `person.location point` column can't be created/read); LANGUAGE C function support -- MISSING (functioncmds.rs only recognizes `language` option string but has no C dynamic-library loading path, blocking `CREATE FUNCTION overpaid(emp) ... LANGUAGE C`) | person, emp, student, stud_emp (test_setup fixtures, but these themselves fail to load due to INHERITS + point) |
| misc_functions | OUT_OF_SCOPE | large grab-bag of builtin admin/introspection SRFs and functions: `num_nonnulls`/`num_nulls` (VARIADIC any), `pg_read_file`/`pg_read_binary_file`/`pg_stat_file`/`pg_ls_dir`/`pg_ls_waldir`/`pg_ls_archive_statusdir`/`pg_ls_summariesdir`, `pg_timezone_names`, `pg_tablespace_databases`, `pg_ls_logicalsnapdir`/`pg_ls_logicalmapdir`/`pg_ls_replslotdir`, `pg_log_backend_memory_contexts`, planner support functions (`ALTER FUNCTION ... SUPPORT`, SupportRequestRows/SupportRequestSelectivity for generate_series), `pg_control_checkpoint/init/recovery/system`, `pg_split_walfile_name`/`pg_walfile_name_offset`, `pg_current_logfile`, `pg_column_toast_chunk_id`, `gist_translate_cmptype_common`, `CREATE FUNCTION ... LANGUAGE C`/`LANGUAGE internal`, `pg_replication_origin_create`, EXPLAIN wrapped in plpgsql, roles/GRANT EXECUTE on function, TOAST STORAGE EXTERNAL | none of the above builtin functions exist anywhere in src/backend (grepped for each name, zero hits outside a single doc-comment mention); commands/explain.rs -- MISSING (EXPLAIN command, used throughout via explain_mask_costs); PL/pgSQL execution -- MISSING (explain_mask_costs is plpgsql); LANGUAGE C / LANGUAGE internal function support -- MISSING (functioncmds.rs has no C-library-loading or internal-symbol-lookup path) | tenk1, pg_settings/pg_stat_activity/pg_tablespace (system views, mostly unpopulated introspection) |
| misc_sanity | NEEDS_MODULES | catalog sanity SELECTs directly against `pg_depend`, `pg_shdepend`, `pg_class` join `pg_attribute`/`pg_index`/`pg_constraint`, `::regtype`/`::regnamespace` casts | src/backend/catalog/dependency.rs -- STUB (dependency recording into pg_depend/pg_shdepend is staged/deferred per module doc comment "TEMPORARY-TODO: awaits the pg_depend index scan once dependency recording lands (M11+)"; pg_depend/pg_shdepend are not populated/queryable as real catalog relations); reg* type casts (regtype/regnamespace) -- MISSING (no regtype/regnamespace cast support found in src/backend/utils/adt) | self-contained (system catalogs only) |
| money | NEEDS_MODULES | `money` type: CREATE TABLE with money column, string/numeric input parsing incl. locale symbols `$`/`,`/parens-as-negative, arithmetic `+ - * /` vs int2/int4/int8/float4/float8/money, comparison ops, `cashlarger`/`cashsmaller`/`cash_words`, casts int4/int8/numeric<->money, overflow/rounding/precision-loss checks, `pg_input_is_valid`/`pg_input_error_info` non-throwing API | src/backend/utils/adt/money.rs -- MISSING (no money/cash module under src/backend/utils/adt at all; only a bare header-stub src/utils/cash.rs with just DatumGetCash/CashGetDatum Datum-conversion helpers, no input/output/arithmetic/comparison/cash_words logic); pg_input_is_valid/pg_input_error_info -- MISSING (not found anywhere in src/backend) | self-contained |
| multirangetypes | OUT_OF_SCOPE | multirange types (`textmultirange`, `int4multirange`, `nummultirange`, `datemultirange`, `tsmultirange`, `tstzmultirange`) input/output incl. canonicalization/normalization, constructors, casts to/from range types (built-in and user-defined range types), set ops (union/intersect/minus), containment/overlap operators, `pg_input_is_valid`/`pg_input_error_info`, GiST/btree opclasses for multirange | src/backend/utils/adt/multirangetypes.rs -- MISSING (no multirange module under src/backend; only header stub src/utils/multirangetypes.rs, all functions unimplemented); src/backend/utils/adt/rangetypes.rs -- MISSING (multirange depends on the base range type machinery; only header stub src/utils/rangetypes.rs exists, not translated); pg_input_is_valid/pg_input_error_info -- MISSING | self-contained |
| mvcc | NEEDS_MODULES | `kill_prior_tuple` index-scan optimization for dead tuples from an aborted subtransaction, `SET LOCAL`, plpgsql `DO $$ ... EXCEPTION WHEN ... END $$` block with custom condition names (`data_corrupted`, `reading_sql_data_not_permitted`) and subtransaction rollback via EXCEPTION, `pg_relation_size()`, `\gset` | PL/pgSQL execution -- MISSING (the whole test body is a single `DO $$ ... $$` block with a FOR loop, BEGIN/EXCEPTION subtransaction, and RAISE ... USING MESSAGE; no PL/pgSQL interpreter exists per project scope notes) | self-contained |

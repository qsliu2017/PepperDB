| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| object_address | OUT_OF_SCOPE | `CREATE FOREIGN DATA WRAPPER`/`SERVER`/`USER MAPPING`/`FOREIGN TABLE`; `CREATE TEXT SEARCH DICTIONARY/CONFIGURATION/TEMPLATE/PARSER`; `CREATE PUBLICATION`/`SUBSCRIPTION`; `CREATE TRANSFORM`; `CREATE STATISTICS`; `CREATE POLICY` (RLS); `PARTITION BY RANGE`; `ALTER DEFAULT PRIVILEGES`; `pg_get_object_address`/`pg_identify_object`/`pg_identify_object_as_address`/`pg_describe_object` catalog-introspection functions over ~40 object-type kinds | Needs several large, wholly-untranslated PG subsystems at once: foreign data wrappers/servers (`src/backend/foreign` -- MISSING), text search DDL (`src/backend/commands/tsearchcmds.rs` -- MISSING/not translated), logical replication publications/subscriptions (`src/backend/commands/publicationcmds.rs`, `subscriptioncmds.rs` -- MISSING), row-level security policies (`CREATE POLICY` -- MISSING), table partitioning (`PARTITION BY RANGE` -- MISSING, per project scope notes), and the generic object-address machinery itself (`src/backend/catalog/objectaddress.rs`'s `pg_get_object_address`/`pg_identify_object` family -- not found anywhere in `src/`, i.e. MISSING). Any one of these blocks the test outright; combined they place it well outside a "few translatable modules" scope. | self-contained (creates its own addr_nsp schema/objects) |

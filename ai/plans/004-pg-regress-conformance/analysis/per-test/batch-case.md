| test | verdict | surface | blocking modules | fixtures |
|---|---|---|---|---|
| case | NEEDS_MODULES | CASE (searched+simple), COALESCE/NULLIF, UPDATE...CASE, EXPLAIN (COSTS OFF), CREATE FUNCTION language plpgsql, CREATE DOMAIN, CREATE OPERATOR, enum_range, arrays, self-contained tables CASE_TBL/CASE2_TBL | (1) src/backend/commands/explain.rs / parser Stmt grammar (STUB/MISSING): `ExplainStmt` absent from `Stmt` production in src/backend/parser/gram.lalrpop -- EXPLAIN cannot be parsed at all, needed for lines 142-149. (2) PL/pgSQL execution, PG subsystem `pl/plpgsql` (MISSING): no pl/plpgsql module under src/backend; CREATE FUNCTION ... language plpgsql bodies (`vol`, `volfoo`, `make_ad`, `ad_eq`) are never executed, needed from line 190 onward. (3) src/backend/commands/*operator* / grammar (MISSING): no `CREATE OPERATOR` production in gram.lalrpop, needed at line 212/236. (4) src/backend/utils/adt/enum.rs (MISSING or STUB): `enum_range` function not found under src/backend/utils/adt, needed at line 259. | self-contained (CASE_TBL, CASE2_TBL created inline) |
| char | NEEDS_MODULES | `char`/`character(1)`/`char(4)` (bpchar) type I/O + comparison ops, `"char"` ad-hoc 1-byte type casts, pg_input_is_valid/pg_input_error_info soft-error API, fixture CHAR_TBL from test_setup.sql | (1) src/backend/utils/adt/varchar.rs (MISSING): PG's varchar.c (bpcharin/out/cmp, the SQL CHAR(n)/CHARACTER(n) type) has no PepperDB counterpart at all; additionally `CHAR`/`CHARACTER`/`VARCHAR` type-name keywords are absent from src/backend/parser/gram.lalrpop, so `char`/`char(4)` column types cannot even be parsed. Blocks nearly the whole file (lines 8-73). (2) src/backend/utils/adt/pg_input.rs or similar (MISSING): `pg_input_is_valid`/`pg_input_error_info` not found anywhere in src/, needed at lines 76-78. Note: the `"char"` ad-hoc type (lines 86-94) is already translated (src/backend/utils/adt/char.rs) and works via text I/O (charrecv/charsend stubs are binary-protocol only, not exercised here). | self-contained (CHAR_TBL is a temp table plus reuse of test_setup's char_tbl fixture, all char(n)/character(1) typed) |
| circle | OUT_OF_SCOPE | CREATE TABLE with `circle` column, circle literal I/O, center()/radius()/diameter()/area() functions, `<->` distance operator, `<` containment operator | access/geometry / src/backend/utils/adt/geo_ops.rs (MISSING): PG's geo_ops.c (circle, point, box, etc.) has no PepperDB translation whatsoever -- no `circle` type registered, no geometry functions/operators exist anywhere in src/. Whole geometry subsystem absent. | self-contained (CIRCLE_TBL created inline) |
| cluster | NEEDS_MODULES | CREATE TABLE (SERIAL, FK), CREATE INDEX, table inheritance (INHERITS), CLUSTER/ALTER TABLE CLUSTER ON/SET WITHOUT CLUSTER, CREATE USER/ROLE, TOAST clustering, declarative partitioning (PARTITION BY RANGE/DEFAULT), expression indexes, external tuplesort via maintenance_work_mem, EXPLAIN (COSTS OFF) | (1) src/backend/commands/tablecmds.rs (STUB): `DefineRelation` hits `not_yet_reachable("DefineRelation: inheritance")` for `CREATE TABLE ... INHERITS (...)`, needed at line 26 -- this panics almost immediately, before most of the file can run. (2) PG subsystem `commands/user` / grammar (MISSING): no `CREATE USER`/`CREATE ROLE` production in src/backend/parser/gram.lalrpop, needed at line 111 (and later `CREATE ROLE regress_ptnowner` at line 238). (3) PG subsystem declarative partitioning, access/partition (MISSING): `PARTITION BY RANGE`/`PARTITION OF`/`DEFAULT PARTITION BY` not supported by the grammar (only window-function `PARTITION BY` exists), needed from line 211. (4) src/backend/commands/explain.rs / parser grammar (MISSING): `ExplainStmt` absent from `Stmt` production, needed at lines 284/286/297/299/310/312. Note: CLUSTER/VACUUM FULL itself (src/backend/commands/cluster.rs) is fully translated (M13); FK enforcement, expression indexes, and external tuplesort are otherwise supported. | self-contained (clstr_* tables created inline); uses `tenk1` fixture at line 258 (`create table clstr_4 as select * from tenk1`) |
| collate | NEEDS_MODULES | inline `COLLATE "name"` clause on columns/expressions/ORDER BY/casts/aggregates, CREATE COLLATION (paren form and `FROM "C"` form), CREATE DOMAIN with COLLATE, collation propagation through CASE/COALESCE/UNION/subqueries, `collation for(...)`, FK with collation, information_schema.views, EXPLAIN (COSTS OFF) | (1) src/backend/parser/gram.lalrpop (MISSING): the `COLLATE` keyword/clause does not appear anywhere in the grammar (no token, no production) -- `b COLLATE "C"`, `text COLLATE "C"`, `ORDER BY b COLLATE "C"` etc. cannot be parsed at all. This blocks essentially the entire file starting at line 18. (2) src/backend/parser/gram.lalrpop (MISSING): `CREATE COLLATION x FROM "C"` (no-parens FROM-form) is not a grammar production -- only `CREATE COLLATION n (deflist)` is supported (line ~1293) -- needed at line 255. (3) src/backend/commands/explain.rs / grammar (MISSING): `ExplainStmt` absent from `Stmt` production, needed at lines 239-242. Note: `CREATE COLLATION` (paren form) DDL itself is implemented (src/backend/commands/collationcmds.rs, catalog/pg_collation.rs). | self-contained (collate_test1/2/4/5/10/20/21/22/23 created inline in schema collate_tests) |
| collate.icu.utf8 | OUT_OF_SCOPE | ICU-provider collations (`COLLATE "en-x-icu"`), inline COLLATE clause, ICU case mapping/collation rules, `\if`/`\gset` psql conditionals gating the whole file on ICU availability | (1) src/backend/parser/gram.lalrpop (MISSING): `COLLATE` clause not in the grammar at all (see collate.sql finding) -- blocks essentially every statement starting at line 20. (2) ICU integration, PG subsystem `utils/adt/pg_locale_icu` / collation provider (MISSING): no ICU binding exists in src/ (only catalog-level `provider=icu` enum value is stored; no actual libicu-backed comparison/case-folding). Even with COLLATE parsing added, ICU-specific collation behavior (accent/case-insensitive rules, `-x-icu` locale names) is a whole unimplemented subsystem. | self-contained (collate_test1 etc., all created inline); test is skipped entirely if the server has no ICU collations registered |
| collate.linux.utf8 | OUT_OF_SCOPE | glibc-provided named locale collations (`COLLATE "en_US"`, "de_DE", "sv_SE", "tr_TR"), inline COLLATE clause, locale-specific string comparison/case mapping, Turkish dotless-i casing | (1) src/backend/parser/gram.lalrpop (MISSING): `COLLATE` clause absent from grammar -- blocks nearly the whole file starting at line 21. (2) libc locale integration, PG subsystem `utils/adt/pg_locale` (MISSING/STUB): no evidence of real glibc locale-backed collation (strcoll/strxfrm per named locale) in src/; only default "C"/"POSIX" behavior is implicitly available. Needed for named-locale comparison and case functions throughout the file. | self-contained (collate_test1 etc.); test is skipped unless running on Linux/glibc with the 4 named locales installed |
| collate.utf8 | OUT_OF_SCOPE | builtin-provider collations (`provider = builtin, locale = 'C'/'C.UTF-8'` i.e. PG_C_UTF8), inline COLLATE clause, Unicode-aware lower/upper/initcap under PG_C_UTF8 special-casing (German sharp-s, Turkish dotless i, Latin ligatures) | (1) src/backend/parser/gram.lalrpop (MISSING): `COLLATE` clause absent from grammar -- `U&'\00C1' COLLATE regress_builtin_c`, `TEXT COLLATE PG_C_UTF8` etc. cannot be parsed, blocking essentially the whole file starting at line 21. (2) builtin-provider Unicode casing, PG subsystem `utils/adt/pg_locale_builtin` / Unicode case-fold tables (MISSING): no PG_C_UTF8 special-casing (unicode_case.c equivalent) or builtin-provider comparison exists in src/; even with COLLATE parsed, the Unicode-aware lower/upper/initcap semantics this test checks are unimplemented. | self-contained (test_pg_c_utf8 created inline); test is skipped entirely if server encoding is not UTF8 |

# PepperDB starting schedule for pg_regress (plan 004).
#
# pg_regress schedule format: `test: NAME [NAME...]` runs the named tests in
# order. The harness pins --max-connections=1 so ordering is deterministic and
# the `regression` database persists across tests within a run (so fixtures
# seeded by `test_setup` are visible to every test after it).
#
# INVARIANT: this schedule is the GREEN baseline -- every line is either PASS or
# an intentional, documented KNOWN-DIFF, so `run.sh` exits 0. It grows as later
# steps translate the modules each blocked test needs. Do NOT force a test green
# by editing upstream expected output.
#
# --------------------------------------------------------------------------
# ACTIVE (PASS today)
# --------------------------------------------------------------------------
# test_setup: the trimmed shared fixtures (INT2_TBL, INT4_TBL, CHAR_TBL,
# VARCHAR_TBL, TEXT_TBL, an EMPTY INT8_TBL, and -- unblocker pass B -- the
# COPY-loaded onek/tenk1 plus onek2/tenk2 via CTAS). run.sh swaps in
# ai/harness/regress/test_setup_pepper.sql for this slot and classifies it by
# "loaded without error" (its output intentionally differs from upstream).
# Must be FIRST so downstream tests see its tables.
test: test_setup
# boolean: the bool type surface (input/output, the six comparison operators +
# casts, IS [NOT] TRUE/FALSE/UNKNOWN, TRUE/FALSE literals, `AS <keyword>` column
# labels, `table.*` expansion, LINE/caret error position, pg_input_is_valid).
# FULL PASS as of step 08: the last line `SELECT * FROM pg_input_error_info(
# 'junk','bool')` -- a record-returning function in FROM (RangeFunction ->
# RTE_FUNCTION -> FunctionScan) -- now matches upstream. (Its known_diff snapshot
# was deleted when step 08 landed the function-in-FROM path.)
test: boolean
# md5: the MD5 hash builtins over text + bytea (RFC 1321 vectors). FULL PASS as of
# step 10 -- md5(text)/md5(bytea) execute and `= <literal>` resolves via the text
# operators + unknown-literal operator coercion the step added.
test: md5
# char, varchar: FULL PASS as of the post-step-10 unblocker pass. The two gaps
# that held them back both landed: (1) temp-namespace shadowing -- CREATE TEMP
# TABLE creates in the per-backend pg_temp_N namespace (created on first use),
# and the active search path checks it FIRST, so the TEMP CHAR_TBL/VARCHAR_TBL
# shadow the test_setup fixtures, DROP TABLE resolves the temp one, and the
# fixture reappears for the "value too long" tail; (2) varchar-vs-literal
# comparison resolution -- a varchar operand with no operator of its own now
# falls through to the text operators via the binary varchar->text coercion
# (parse_oper) instead of erroring. (The `||` operator is also lexed + resolves
# now; these two tests don't use it.)
test: char
test: varchar
# comments: FULL PASS as of unblocker pass B -- needed (1) nested /* */ block
# comments in the lexer (scan.rs skip-callback), (2) unknown-literal output
# columns resolved to text (PG resolveTargetListUnknowns in analyze), and
# (3) `AS second` (SECOND_P keyword) -- the full-kwlist terminal coverage.
test: comments
# portals_p2: FULL PASS as of unblocker pass B -- it only needed the onek
# fixture (COPY FROM file + `name` columns) restored in test_setup.
test: portals_p2
# select_having: FULL PASS as of unblocker pass E (2026-07-02) -- HAVING grammar
# (gram.y having_clause), transformed like WHERE (EXPR_KIND_HAVING; aggregates
# allowed), checked by parseCheckAggregates (PG-format `column "rel.col" must
# appear in the GROUP BY clause...` with LINE/caret), evaluated as the Agg node's
# qual per finalized group (setrefs numbers tlist+qual Aggrefs; nodeAgg skips
# HAVING-rejected groups); the degenerate no-agg/no-GROUP-BY form plans as a
# childless gating Result (PG create_degenerate_grouping_paths -- the table is
# never scanned). Also needed pass D/E catalog+cache work: int8 operator rows,
# count("any") resolution, lower(bpchar), HAVING cache warming.
test: select_having
# select_implicit: FULL PASS as of unblocker pass E -- the resjunk GROUP BY /
# ORDER BY planner path: grouping EXPRESSIONS (b/2, lower(c), a%2) are scan/join
# output columns (make_scan_input_tlist keeps sort-keyed exprs whole; a JOIN top
# node reprojects them via apply-scanjoin-target), setrefs matches whole
# expressions against the child tlist (search_indexed_tlist_for_non_var) with
# parent-first upper fixup, nodeAgg emits the FULL tlist (junk stripped by the
# top junk filter), and the SQL92 findTargetlistEntry rules landed (output-name
# matching with ambiguity check, GROUP BY prefers FROM columns via colNameToVar,
# `GROUP BY/ORDER BY position N is not in select list` with LINE/caret). Node
# equality now ignores Var/Const parse locations (PG equal()), so `GROUP BY a/2`
# matches the tlist `a/2`.
test: select_implicit
# int2, int4: FULL PASS. Unblocker pass D landed the entire catalog/adt surface
# (full int2 + int24/int42 + int8 operator sets, big literals -> int4/int8/
# numeric consts, int2vector, the int8/float8/numeric->int2/int4 casts, per-type
# cache warming); pass E landed the last four grammar/lexer forms it needed:
# `<<`/`>>` operator tokens (lexer + the qual_Op tier), alias column lists
# `[AS] f(a, b)` (with PG's "table has N columns available but M specified"
# error), `TABLE t` as a simple_select (so `(TABLE t)` parses), and sub-SELECT /
# `(VALUES ...)` in FROM (RangeSubselect -> RTE_SUBQUERY; the planner's existing
# pull_up_subqueries flattens the simple shapes to base VALUES/relation RTEs, so
# no SubqueryScan is needed). Also un-guarded: makeConst over varlena (numeric
# consts) -- no TOAST exists, so the PG detoast is an identity here.
test: int2
test: int4
#
# Step 08 also landed: generate_series (int4/int8) as a materialized SRF-in-FROM,
# gcd/lcm(int8), and the funcapi SRF ValuePerCall machinery. The step's remaining
# target tests are STILL blocked by other modules and are left OUT (not KNOWN-DIFF)
# per plan policy:
#   select_distinct_on  [needs DISTINCT ON grammar + the `onek` fixture]
#   int8                [needs the INT8_TBL table + int8 typmod/`::int8` casts]
#
# Step 09 landed CREATE TABLE AS / SELECT INTO (the DR_intorel receiver: create the
# target from the query rowtype + insert the materialized rows; WITH [NO] DATA;
# SELECT ... INTO rewrite; IF NOT EXISTS with NOTICE-skip; "already exists" error).
# Verified via psql: CTAS/SELECT-INTO over a TABLE source works end-to-end. Its target
# tests still need LATER modules and stay OUT (verified NEW-DIFF), each blocker below:
#   select_into      [needs, in combination: SRF-in-target-list execution
#                     (`SELECT generate_series(1,3)` w/o FROM -> ProjectSet, a
#                     PRE-EXISTING executor gap, not this step), EXPLAIN ANALYZE
#                     (step 14), roles + SET SESSION AUTHORIZATION + ALTER DEFAULT
#                     PRIVILEGES (step 18), executable SQL functions + DO/PL (step 23),
#                     the int8_tbl fixture, and the CTAS explicit column-name list
#                     `(a) AS` (DEFERRED here to keep the LALR(1) table from blowing up
#                     -- CreateStmt/CTAS are unified; the `( cols )` list collides with
#                     the `( elts )` element list). Left OUT until those land.]
#   select_distinct  [onek fixture NOW LOADS (pass B). Verified 2026-07-02, still
#                     blocked by: `SELECT DISTINCT ... FROM (subquery)` -- the live
#                     SUBQUERY-RTE / SubqueryScan planner path (now a catchable
#                     0A000 instead of a dropped connection) -- plus the CTAS
#                     `AS SELECT DISTINCT g%1000 FROM generate_series(...)` lines.
#                     Left OUT.]
#
# --------------------------------------------------------------------------
# BLOCKED -- verified NEW-DIFF against the current server (2026-07-01), each
# owned by a later step. Left OUT (not KNOWN-DIFF'd) per plan policy: the
# schedule stays a true green baseline; add each back in the step that unblocks
# it. Empirical blocker in brackets.
# --------------------------------------------------------------------------
# Tier-0 "READY" from static analysis -- comments + portals_p2 went ACTIVE in
# unblocker pass B (2026-07-02); the rest re-verified NEW-DIFF:
#   sanity_check    [catalog relations are not SQL-queryable at all yet
#                    ("relation pg_class does not exist" over the wire); then the
#                    LEFT JOIN + `~` regex over catalogs + indkey[0] subscripts]
#   misc_sanity     [pg_depend / pg_shdepend do not exist; aclitem[]/anyarray
#                    catalog column output]
#   combocid        [`SELECT ctid,cmin,*` system columns don't resolve; the
#                    `INSERT ... SELECT 1 LIMIT 0` source needs SubqueryScan (a
#                    live un-pullable SUBQUERY RTE -- now a catchable 0A000, was
#                    a dropped connection); then the aborted-txn cascade]
#   bitmapops       [CREATE TABLE ... WITH (autovacuum_enabled = false) --
#                    reloptions, step 13; then index/bitmap planner paths]
# Tier-1 simple types -- all NEW-DIFF:
#   int8            [INT8_TBL now LOADS WITH ROWS in test_setup (pass D fixed big
#                    integer literals + numutils-exact int8in); int8.sql itself
#                    still needs its own operator/cast tail -- re-verify when
#                    picked up]
#   float4, float8  [float/numeric input -- numeric step (12); T_Float literal now
#                    a catchable ERROR (no longer crashes) but not yet computed]
#   text            [`||` NOW LEXES and text||text resolves (textcat). Verified
#                    2026-07-02, text.sql still needs: the cross-type concat
#                    operators `text || anynonarray` / `anynonarray || text`
#                    (textanycat/anytextcat, polymorphic-arg resolution),
#                    concat()/concat_ws()/format() execution which stages on
#                    get_call_expr_argtype (variadic-"any" arg-type resolution, a
#                    PRE-EXISTING executor gap) + the VARIADIC grammar, E'...'
#                    escape strings + leading block comments (not lexed), the
#                    "function length(integer) does not exist" arg-typed error
#                    format with LINE/HINT, the TEXT_TBL fixture, to_date
#                    (datetime step 11), and string_agg/array_agg (aggregate
#                    steps). TEXT_TBL now LOADS in test_setup (pass B); block
#                    comments now lex. Left OUT until the rest lands.]
#   name            [pass B landed: `name` as a column type + `::name` casts
#                    (NAME_P in ColId/TypeFunctionName), the name comparison
#                    operator seeding + warm-set coverage, and btree sortsupport.
#                    Re-verified 2026-07-02, name.sql still needs: parse_ident
#                    (text[] result -> array builder + `a[1]` subscripting +
#                    `::name[]` casts), E'...' escape strings (type "e" errors),
#                    `~` over name (operator not seeded for (19,unknown)), and
#                    repeat()/format(). Left OUT.]
#   strings         [Step 10 landed regexp_* scalars (regexp_replace/match/like/
#                    count/instr/substr), LIKE/ILIKE, encode/decode helpers, and the
#                    Spencer-style regex engine; `||` now lexes. strings.sql is XL
#                    and still blocked by: bytea bit-ops (get/set_bit/byte),
#                    SIMILAR TO, the SRF regexp_matches/split_to_table (SRF
#                    machinery), unistr, and bytea<->int casts. Left OUT.]
#   union           [pass E landed ORDER BY/LIMIT over a set-op tree (analyze
#                    resolves the sort against the output columns SQL92-style;
#                    the planner stacks Sort/Limit above the combined plan, with
#                    the =/</> caches async-warmed) -- the first 7 integer UNION
#                    queries now match. Re-verified 2026-07-02, the rest of
#                    union.sql is blocked by: `SELECT 1.1 UNION ...` numeric
#                    literals (a varlena numeric Const hits the "makeConst:
#                    varlena detoast deferred" grow-guard and the panic KILLS the
#                    server -- numeric step 12), EXPLAIN (step 14, ~40 lines),
#                    subquery-in-FROM (SubqueryScan), enable_hashagg/sort GUCs,
#                    varbit/arrays/records/xid, TRIM(TRAILING FROM), CREATE TYPE
#                    AS, inheritance, plpgsql. Left OUT.]
#   delete          [pass E landed the grammar/analysis legs: inline column
#                    constraints (PRIMARY KEY / UNIQUE / [NOT] NULL ->
#                    transformColumnDefinition splits an implicit unique-index
#                    IndexStmt; ProcessUtilitySlow runs it) and the DELETE alias
#                    forms (relation_expr_opt_alias; the alias is exposed instead
#                    of the table name, with PG's "invalid reference to
#                    FROM-clause entry" + "Perhaps you meant to reference the
#                    table alias" hint). Re-verified 2026-07-02, still blocked by:
#                    `id SERIAL` (type "serial" does not exist -- needs the
#                    serial->sequence+nextval-default expansion in
#                    transformColumnDefinition over the pg_attrdef default
#                    machinery) and `repeat('x', 10000)` (a 10KB tuple -> "row is
#                    too big"; needs TOAST/inline compression, step 29 -- the
#                    plan itself lists `delete` as step 29's test). Left OUT.]
